# Claude entry point

Read and follow `AGENTS.md` for the shared workflow and role boundaries.

Before starting or resuming, read `PRD.md`, `STATUS.md`, `DECISIONS.md`, and the current milestone handoff named in `STATUS.md`. Inspect branch, HEAD, and working-tree changes, then summarize the milestone and next action.

Your role is implementation and tests, including review fixes. Follow the shared handoff process; do not begin a milestone that is not READY. Keep shared instructions in `AGENTS.md` rather than duplicating them here.
