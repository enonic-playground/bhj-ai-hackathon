# M5 — Mobile usability and PWA

## Brief — Codex

- State: READY.
- Owner: Claude (implementation); Codex reviews the committed checkpoint.
- Prepared: 2026-09-21.
- Specification: PRD v1.0 sections 3–8, accepted wireframes, D015 visual feedback, D016 audio exclusion, and D020 below.
- Repository/branch: `/Users/bhj/Documents/bhj-ai-hackathon`, `main`.
- Reference base commit: `0509af884bd895451d248fee1ad6e00a337c5324` (M4 acceptance).
- Last accepted implementation: `f4708cbaf1f403854a7b083a444e81d64a6f8d62`. M4 is ACCEPTED; M4-R1/R2 are closed.
- Baseline verification: 263 unit/integration tests, 69 browser tests with 3 intentional skips, both builds; two temporary production denied-storage probes passed independently. Those probes are not retained repository tests.
- Estimate: PRD allowance **1.5–2 focused hours**, with existing project contingency unchanged. This includes substantial device/update verification; flag an overrun or unavailable device honestly rather than dropping checks. Record Claude and Codex work/session time and tokens separately where available; unavailable counters stay unavailable.

### Starting checkpoint and ownership

The checkout was clean at planning start. Checkpoint this brief and its STATUS/DECISIONS/PRD planning edits separately before implementation, or use the owner's planning commit; record the actual full implementation base SHA. Preserve unfamiliar changes. Claude marks IMPLEMENTING when work starts. A file handoff does not launch the other agent.

The existing Pages workflow publishes ordinary `dist/` on push to `main`; do not push, deploy or change Pages settings as part of this brief. Local production testing must reproduce `/bhj-ai-hackathon/` as well as root hosting. The current Vite base is `./`; new worker, manifest and icon URLs must preserve subpath compatibility.

### Objective and boundaries

Deliver a usable mobile/desktop application that can be installed where supported and can launch and play the complete campaign offline after one successful online preparation. Updates must preserve an active run, including a run in another tab.

In scope: responsive layout and touch refinements, keyboard/focus and readable feedback, system reduced-motion support, original app icons/manifest, production service worker and complete offline assets, safe update lifecycle, retained storage-failure browser tests, installation/recovery documentation and device evidence. Extend the existing shell, renderer, input adapters and build/test setup; keep browser/PWA concerns outside the simulation.

Out of scope: audio/music/sound/mute (excluded permanently), new gameplay, accounts/backend, analytics, saving a run, new mazes, optional swipe gestures when the visible pad suffices, and a new settings panel. Preserve all M1–M4 rules, word/scoring values, ball visibility and default approved enemy feedback. Full release cleanup, remaining fruit-edge regression expansion and three human release runs remain M6; collect useful human pacing observations now if available.

### Implementation contract

1. **Responsive layout and touch.** At 360 × 640 CSS pixels show HUD, square-tile maze and directional pad together in CHASE; A–Z remains usable in GUESS. Support landscape (including 640 × 360), desktop 1280 × 800 and a tall-to-short desktop resize without reload. Account for safe-area insets and changing available height. Do not shrink touch buttons below 44 × 44 pixels or remove the pad on touch landscape. Preserve page gestures outside gameplay controls. Handle interrupted pointers, resize/orientation, blur and background without stuck input, unintended guesses or elapsed-time catch-up. At larger text/zoom, permit vertical scrolling in menus/frozen guessing panels while keeping all actions reachable and avoiding horizontal page overflow. Test long categories, eight-letter words, many misses, five-digit score, four lives/earned-life badge, and both new result panels—not only the starting HUD. Reuse narrow injected/gated fixtures for hard-to-reach UI states; never add production state setters or answer leakage.
2. **Focus and announcements.** Menus/overlays own actionable focus; covered/hidden controls cannot receive Tab input. Maintain visible focus, logical tab order, labeled HTML buttons and non-color-only status. Check title, pause, guess, countdown, death, level-complete, game-over and campaign-complete transitions, including touch switching to keyboard. Retain explicit Resume after returning from background. Word/guess and extra-life feedback must remain understandable without announcing every animation frame. Install/offline/update notices must not steal focus or cover gameplay controls. Full nonvisual navigation of the action maze remains outside scope.
3. **Reduced motion.** Respect `prefers-reduced-motion` on initial load and when it changes. Apply it to canvas effects as well as CSS. In reduce mode use a stable contrasting ball fill with its white ring, steady pellet/shield indicators, a nonflashing frightened-expiry cue, and suppress optional decorative oscillation/transitions; essential actor movement remains unchanged. In normal mode retain D015's smooth 2,000 ms active-time hue cycle and owner-approved frightened warning. Presentation changes must not affect speed, timers, input, collision or scoring. No persisted preference/UI toggle is necessary; use the OS preference. Test both modes and changing preference while paused/guessing without advancing the maze.
4. **Install metadata and assets.** Provide manifest name/short name, stable app identity, scoped start URL, standalone display, suitable colors and correctly sized original icons (192/512 PNG plus a maskable asset and Apple touch icon where used). Verify actual dimensions and safe artwork placement. All URLs must resolve within the deployed app path; installed launch returns to TITLE, not a saved run. Avoid copyrighted game artwork and external fonts/CDNs. Keep installation progressive: ordinary browser play works if install or service-worker APIs are unavailable. Offer concise title-screen install help; show an install action only when the browser supports the corresponding prompt. Do not invent a universal install dialog. Verify current platform-specific instructions during implementation and document tested browser/OS versions.
5. **Complete offline preparation.** Register the worker only for ordinary production output. Precache a coherent version of the shell, all required JS/CSS, bundled words, artwork, icons and manifest; build-derived asset enumeration must follow hashed filenames. A successful first online load must be enough to prepare the entire five-level run, not only the currently visited level. Define readiness from successful installation/cache completion, not `navigator.onLine` or registration alone; show a quiet ready/unavailable status on the title screen. Test offline reload and a fresh page launch in the same prepared browser profile, then play. First-ever offline visits cannot install uncached assets; document that limitation and possible browser cache eviction. Worker/CacheStorage failure must leave online play functional and must not falsely claim offline readiness. Cache only appropriate app-local GET requests, scope navigation fallback to the application, and do not answer missing scripts/icons with HTML. Namespace caches by app path and version; never delete unrelated origin caches or local high score.
6. **Safe version updates.** Install a new complete version into separate caches; a failed/partial download leaves the prior playable version intact. No unconditional `skipWaiting`, reload-on-controller-change or automatic reload during play. For this milestone, prefer the native waiting lifecycle: notify at TITLE or after GAME_OVER/CAMPAIGN_COMPLETE, explain that closing all Hac-Man windows and reopening applies the update, and activate when old controlled clients are gone. LEVEL_COMPLETE is mid-run and PAUSED retains an active run; neither is a safe forced-reload boundary. Keep old assets usable until no client needs them. If choosing an explicit Update now action instead, document and test coordination with every controlled tab/window; a hidden, unresponsive or active client must defer activation. A title tab must never discard another tab's run. Newly activated versions must launch coherently and work offline. Keep worker URL stable; version content/caches instead. Do not cache the worker script via its own application asset fallback.
7. **Storage and build isolation.** Retain committed browser regressions for a throwing localStorage getter and quota-exceeded writes: game starts/scores, in-memory best survives return to title, reload returns to title without crashing, and normal/corrupt persistence still works. Preserve M4's stale-tab score regression. Dev and fixture builds must not register the production worker; use separate origins/profiles for fixture and production checks so an old worker cannot disguise a broken build. Ordinary production must still contain no fixture parameter controls. Any new PWA build tooling must be pinned, documented and covered by the lockfile; avoid unrelated upgrades.
8. **Documentation and evidence.** Update README for actual install paths, first-online preparation, offline behavior, pending updates, app-specific cache recovery (including whether clearing site data loses best score), build/preview/test commands, architecture and limitations. Use `docs/evidence/m5/` for new screenshots; preserve accepted M1–M4 evidence. Record exact browser/device/OS, viewport, build SHA, manual versus automated steps, observed results and unavailable checks. Check smoothness on a representative laptop and phone and record observations without claiming measured 60 fps from emulation. Record any human first-capture/per-level/run timings separately from scripted timings; PRD targets remain unchanged.

### Working increments

Keep the application runnable after each increment: (1) layout/focus/reduced motion and storage regressions; (2) manifest/icons and complete production offline preparation; (3) safe updates with two-version/two-tab tests; (4) device demonstrations and README. Claude may choose a small service worker or build plugin, but must record the choice and explain how emitted assets and updates are tested. Do not introduce a second gameplay state machine.

| ID | Acceptance criterion | Required evidence |
| --- | --- | --- |
| AC1 | CHASE/GUESS, earned-life HUD and all result actions fit or remain reachable at required sizes; no horizontal overflow or undersized targets. | Desktop/mobile/landscape, long-content, text scaling and height-shrink browser checks plus screenshots. |
| AC2 | Touch/keyboard focus, pause/background and transitions remain correct with readable non-color feedback. | Input/focus regression journeys; real-phone touch/orientation observations. |
| AC3 | OS reduced motion changes decorative effects only; default D015 appearance and active-time behavior remain intact. | Renderer/preference tests and browser media emulation including runtime change; visual evidence. |
| AC4 | Manifest/icons/start URL support installed launch inside the app path, with accurate install help and browser fallback. | Parsed manifest/asset checks; root/subpath loading; actual installation evidence by platform. |
| AC5 | After one completed preparation, production reload/new launch and all five levels work offline with local assets. | Fresh-profile online preparation then disconnected production journey, including subpath and reload; request/cache evidence. |
| AC6 | Updates never interrupt any active tab; complete new version takes over at a safe boundary and remains offline-capable. | Distinct A/B production builds on the same origin/scope, active and paused runs, two tabs, failed install, later activation and offline B launch. |
| AC7 | PWA/storage failures preserve online play; high score remains resilient; fixture/dev and unrelated origin caches are isolated. | Retained denied/corrupt storage tests, registration/cache failure checks, cache namespace and fixture-isolation checks. |
| AC8 | Existing gameplay stays functional through presentation and PWA changes. | Full typecheck/unit/integration/browser suites and production build smoke, including all M4 scoring/campaign cases. |
| AC9 | Documentation and device evidence accurately describe the delivered PWA and its limits. | README, AC evidence map, tested device matrix, manual exit demonstration, explicit unavailable checks and timing attribution. |

### Required verification and exit demonstration

Run existing commands: `npm ci`, `npm run typecheck`, `npm test`, `npm run build`, `npm run build:fixture`, `npm run test:e2e`. Add/document narrowly scoped production PWA tests/scripts if necessary; do not claim uncreated commands exist. Preserve refusal to reuse stale preview servers. Run Node 22.12.0 (Pages runtime) when available; otherwise explicitly mark it unverified. PWA behavior must be checked on built ordinary production, not Vite dev or only the fixture build.

Use isolated test profiles and a production server capable of serving the real repository subpath. For offline campaign evidence, retain real controls and production rules: a test can know the bundled candidate bank and guess letters/chase repeatedly, but must not enable fixture parameters, reveal an unsolved answer through runtime APIs or mutate gameplay. Existing deterministic fixture campaign tests remain useful regression evidence but cannot replace production offline verification. Wait on worker/cache readiness conditions instead of an arbitrary sleep. Turn off network and verify required assets cannot be rescued from the server.

For updates, serve build A, prepare/control it, keep a run active, then serve distinguishable build B at the same origin and scope. Discover B without reloading the active page; confirm pending status and unchanged run. Repeat with another tab at TITLE while the first is active/paused. Close old app clients or use the proven safe action, reopen B, verify its version and offline operation. Simulate a failed B precache and prove A still launches/plays; do not substitute a mocked update flag for a real worker lifecycle test.

Manual exit: on available desktop and phones, install from secure hosting, launch standalone, play catch → correct → wrong → catch → solve, background/resume, rotate, then disconnect and relaunch/play. Check Android, iOS and desktop installation where supported; record platforms unavailable to the session as UNVERIFIED with exact owner-run steps. Browser emulation is not device proof. If required device evidence cannot be obtained, submit the implementation for review with AC2/AC4/AC9 device portions explicitly unverified and name the resumption dependency; do not self-declare all criteria Met or final acceptance. Complete independent implementation/testing work before requesting any owner-run validation. Final milestone acceptance remains Codex's review decision under the shared workflow.

### Technical references

Consulted for planning; verify chosen implementation details against current documentation. The browser's worker waiting lifecycle protects existing clients; changing manifest scope does not by itself set worker scope. CSS reduced-motion preferences also need to inform canvas rendering.

- [Service worker lifecycle](https://web.dev/articles/service-worker-lifecycle)
- [Manifest scope](https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Manifest/Reference/scope)
- [Reduced-motion preference](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@media/prefers-reduced-motion)

## Implementation handoff — Claude

- Base commit: `19bb0529bccd0fc7aaf07b7cc4bb6347fda8950a` (the M5 brief commit; the checkout was already clean at planning start, so no separate planning checkpoint was needed).
- Implementation commit: `<recorded in the following documentation commit — see STATUS.md>`. Review that exact base-to-implementation diff.
- State: REVIEW. Ownership transfers to Codex; the project owner triggers the review.
- Working tree outside the implementation commit: only this record and the `STATUS.md` update, in a following documentation commit. Nothing else is uncommitted.

### Summary

Every piece extends the existing DOM-free-simulation/shell/renderer architecture rather than adding a parallel one, matching D020's boundary: no gameplay rule, timing, collision or score value changed.

**Reduced motion.** `src/render/motion.ts` factors the four decorative canvas effects — the D015 ball hue cycle, the power-pellet pulse, the frightened-expiry flash, the protection-ring pulse — into pure functions taking a `reducedMotion` boolean, so they are unit-testable without a canvas (`tests/motion.test.ts`); `MazeRenderer` calls them through one `setReducedMotion()` setter. `src/app/reducedMotion.ts`'s `ReducedMotionWatcher` wraps `matchMedia('(prefers-reduced-motion: reduce)')` including its `change` event, so a runtime OS change takes effect without a reload; `src/styles.css` adds a matching global CSS override for any transition/animation. Nothing here touches speed, timers, input, collision or scoring.

**Layout and focus.** `#app`'s safe-area padding now covers all four insets, not only the bottom; a new `.pwa` block (styles alongside the existing overlay/panel rules) holds the install/offline status without ever taking focus. The existing 360 × 640 stacked layout, 820 px+ two-column layout, focus management and 44 px targets (all M1–M4 work) are otherwise unchanged and re-verified rather than rebuilt; new coverage (below) exercises landscape, an eight-letter word, a long category, several misses, a five-digit score, the earned-life badge, and both of M4's newer result panels, none of which M1–M4's own suites happened to exercise together.

**Manifest and icons.** `public/manifest.webmanifest` (relative `start_url`/`scope`/icon paths, so it resolves correctly at the origin root or a subpath) and `public/icons/*.png`, generated by `scripts/generate-icons.mjs` — a hand-rolled PNG encoder (Node's own `zlib.deflateSync`, a hand-written CRC32) with no image-processing dependency, drawing this project's own established player-disc/ball-ring shapes rather than sourced artwork. `index.html` links the manifest, icons and a `theme-color`.

**Service worker and offline.** `sw/service-worker.template.js` is the worker's source; `vite.config.ts`'s `hacmanServiceWorkerPlugin` (Vite's own `configResolved`/`closeBundle` hooks, Node's built-in `fs`/`crypto` — no new dependency) fills in the real, hashed precache list from what the production build actually wrote and a version hash derived from that list plus `index.html`'s own content, writing `dist/sw.js` only when `mode === 'production'`. Every cached URL is relative to the worker's own script location, so the same output works at the origin root or a subpath with no build-time path knowledge. `src/app/pwa.ts` (DOM/service-worker wiring) and `src/app/pwaStatus.ts` (pure text/classification helpers, unit-tested in `tests/pwaStatus.test.ts`) register the worker only in the ordinary production build (`import.meta.env.PROD && !__TEST_FIXTURES__`) and reflect real lifecycle events as a quiet title-screen status line, never claiming readiness from registration alone.

**Safe updates.** The worker never calls `self.skipWaiting()` or `clients.claim()` anywhere, which alone is what makes the native waiting lifecycle work: a new version installs into its own cache and sits `waiting` until every client controlled by the previous version has closed, entirely through the platform's default behaviour. A failed/partial `cache.addAll()` during install rejects atomically, leaving the previously active version untouched. Cache names are namespaced by the worker's own `registration.scope` path, so an unrelated app at a different subpath on the same origin cannot collide with this app's cache cleanup.

**A real bug the test suite caught.** While testing offline reload, the app's own JS/CSS (loaded with `crossorigin`, so requested with an `Origin` header) failed to be served from cache even though the exact same URL was demonstrably present in `CacheStorage`, because `vite preview`'s static server sends `Vary: Origin` and the worker's own internal `cache.addAll()` fetch did not reproduce that header identically. Fixed by matching with `{ ignoreVary: true }` everywhere (this cache only ever holds the worker's own precached output, never negotiated third-party content, so ignoring `Vary` is the correct general policy, not a narrow workaround). See [D021](../DECISIONS.md#d021--m5-implementation-choices).

**A new, narrow fixture parameter.** `testBankWord=<index>` (`src/app/fixture.ts`) pins every level's word to the real 50-entry `WORD_BANK` instead of the 8-entry `SEED_WORDS` list `testWord`/`testWords` already use — added because `SEED_WORDS`'s longest entry is 7 letters, so no existing parameter can render a genuine eight-letter word/category, and dozens of committed M2–M4 tests depend on `testWord`/`testWords`' exact existing indices. `testWord`/`testWords` and every test using them are unchanged.

### Acceptance criteria

| ID | Assessment | Evidence |
| --- | --- | --- |
| AC1 | Met (automated); device text scaling/zoom UNVERIFIED | `e2e/layout.spec.ts` (inherited, still passing: 360 × 640, tall-to-short resize) plus new `e2e/layout-content.spec.ts`: a 640 × 360 landscape journey, an eight-letter word (`COMPUTER`/`Technology`, via `testBankWord`) with several real misses and both the level-complete and campaign-complete panels, and a five-digit score/four-life/earned-life-badge HUD check. No horizontal overflow and no sub-44 px target in any of these. Real OS-level text scaling/zoom was not exercised; browser emulation is not a substitute. |
| AC2 | Met (automated); real-phone touch/orientation UNVERIFIED | Inherited `e2e/guessing.spec.ts`, `e2e/background.spec.ts`, `e2e/arcade.spec.ts` touch journeys (focus order, hidden-panel tab order, pause/background transitions, non-color feedback) all still pass, now alongside the reduced-motion and PWA-notice additions, neither of which calls `.focus()` (see D021). Real-device touch/orientation is **UNVERIFIED** — no physical phone available this session. |
| AC3 | Met | `tests/motion.test.ts` (pure functions) plus `e2e/reducedMotion.spec.ts`: the ball fill holds one stable colour under `page.emulateMedia({reducedMotion:'reduce'})` across a full D015 cycle, a runtime preference change takes effect without reload (verified by canvas pixel sampling both ways), and toggling the preference while guessing is frozen leaves the snapshot (score, revealed letters, status) byte-identical. |
| AC4 | Met (automated: manifest/icons/subpath); real install UNVERIFIED | `e2e/pwa.spec.ts`'s manifest test parses the served manifest, confirms `display: standalone`, a maskable icon, and that every declared icon actually resolves at its declared square PNG dimensions (read from the real IHDR chunk, not just an HTTP 200); the app's existing `base: './'` (unchanged) and the worker's scope-relative URLs together are what root/subpath compatibility depends on. Install help text and the conditional install button are implemented (`src/app/pwaStatus.ts`, `src/app/pwa.ts`) but real installation on Android/iOS/desktop is **UNVERIFIED** — no physical device or a secure (HTTPS) hosted deployment reachable from a real browser this session. |
| AC5 | Met | `e2e/pwa.spec.ts`: a fresh profile registers, precaches (asserted directly against `CacheStorage`: `index.html`, the manifest, a JS chunk and an icon are all present), then — offline — both a reload of the preparing tab and a brand-new tab in the same profile load and play a real round with real scoring; a missing precached asset is never answered with the HTML shell; a profile that starts offline before any online visit fails visibly (documented limitation) rather than pretending to work. |
| AC6 | Met | `e2e/pwa-update.spec.ts`: two independently built, genuinely distinguishable production versions served from one dedicated static server at the same origin/scope (not a mocked flag). An active tab's real, running run (score rising, status `chase`) and a second tab at TITLE are both left completely undisturbed while the new version installs and becomes `waiting`; only once both tabs are closed and a fresh one opens does the new version activate, and it then works offline too. A separate build with one precached asset deleted (a simulated failed deploy) is confirmed to leave the previously active version serving normally, with no `waiting` worker ever appearing. |
| AC7 | Met | `e2e/storage.spec.ts` (new, retained repository tests — M4 review's two probes were temporary and not committed): a throwing `localStorage` getter and quota-exceeded writes are each exercised against the production build for — the game starts and scores; the in-memory best survives a return to title; a reload returns to title without crashing; and, separately, a genuinely working backend still persists across reload while corrupted stored data reads back as zero rather than crashing. `e2e/pwa.spec.ts`'s production-carries-no-fixture-controls-style checks plus the fixture build's own missing `sw.js` (confirmed: `dist-fixture/` contains no `sw.js`, so it cannot register one) cover fixture/dev worker isolation. |
| AC8 | Met | Full suite green: 292 unit/integration tests (up from 263), both production and fixture builds, and 95 browser tests passed with the same 3 intentional touch-only skips as M4 (up from 69/3), across all four projects. This includes every inherited M1–M4 journey (arcade, campaign, five-level completion, storage-corruption, ball-colour, background/pause) unmodified in substance. |
| AC9 | Met (documentation); device matrix mostly UNVERIFIED | This handoff, `README.md`'s new "Progressive web app" section and updated "Known limitations", [D021](../DECISIONS.md#d021--m5-implementation-choices), the AC table above, and the device matrix and manual-exit sections below name exactly what is and is not verified, and by what method. |

#### Device matrix (AC2/AC4/AC9)

| Platform | Install | Offline | Touch/orientation | Method |
| --- | --- | --- | --- | --- |
| Desktop Chromium (headless, via Playwright) | Automated: manifest/icons/service-worker lifecycle verified real | Automated: real `CacheStorage`, real offline reload/fresh-tab, real update lifecycle | N/A | `e2e/pwa.spec.ts`, `e2e/pwa-update.spec.ts`, `e2e/storage.spec.ts` |
| Desktop Chrome/Edge (real browser) | **UNVERIFIED** | **UNVERIFIED** | N/A | Owner-run: serve `dist/` over HTTPS (or `npm run preview` for a same-machine check), visit, use the address-bar/menu install action, then disconnect networking and reload. |
| Android (Chrome) | **UNVERIFIED** | **UNVERIFIED** | **UNVERIFIED** | Owner-run: host `dist/` over HTTPS, visit on a phone, use the "Install app"/"Add to Home screen" prompt or menu action, then enable airplane mode and relaunch from the home screen icon. |
| iOS (Safari) | **UNVERIFIED** | **UNVERIFIED** | **UNVERIFIED** | Owner-run: host `dist/` over HTTPS, visit in Safari, Share → Add to Home Screen (no `beforeinstallprompt` exists on iOS, so only the manual path applies), then enable airplane mode and relaunch from the home screen icon. |

No physical device and no browser GUI were available in this session; only headless Chromium via Playwright, against real built production output and a real worker, was available. This automated evidence is real worker/cache behavior, not a mock, but it is not a substitute for the device checks above, which the brief explicitly requires and which remain the named resumption dependency for full M5/PRD release-gate acceptance.

### Verification

All on Node 26.7.0 (npm 11.19.0), macOS arm64. Node 22.12.0 (the advertised minimum and the Pages workflow's runtime) was not available in this session, so it is **not** re-verified for M5; the declared engine range and lockfile are unchanged.

- `npm run typecheck` — passed.
- `npm test` — passed, 292 tests in 25 files (263 before this milestone; new files: `tests/motion.test.ts`, `tests/reducedMotion.test.ts`, `tests/pwaStatus.test.ts`, `tests/fixture.test.ts`).
- `npm run build` — passed. `dist/index.html` 10.27 kB (gzip 2.63 kB), `dist/assets/*.js` 43.48 kB (gzip 15.16 kB), `dist/assets/*.css` 7.30 kB (gzip 2.10 kB); `dist/sw.js` generated with 8 precached files.
- `npm run build:fixture` — passed. Confirmed no `sw.js` is emitted into `dist-fixture/`.
- `npm run test:e2e` — passed, 95 browser tests plus the same 3 intentional touch-only skips as M4 (69/3 before this milestone), across `desktop`, `mobile` and `production`. New production-only files (`e2e/pwa.spec.ts`, `e2e/pwa-update.spec.ts`, `e2e/storage.spec.ts`) were added to `playwright.config.ts`'s production-only `testMatch` pattern alongside the existing `production.spec.ts`, so they run once against real built output rather than three times against the fixture build. Run twice end to end after the final change, both clean; `e2e/pwa-update.spec.ts` was additionally run standalone several times to check for the flakiness a real worker-lifecycle timing test could have, with none observed.
- Production PWA behaviour was checked against `npm run build && npm run preview`, never the dev server or the fixture build, per the brief.

### Decisions and deviations

Recorded as [D021](../DECISIONS.md#d021--m5-implementation-choices): the generated-not-hand-maintained service worker and its `configResolved`/`closeBundle`-based Vite plugin, the deliberate absence of `skipWaiting`/`clients.claim`, the `{ ignoreVary: true }` cache-matching fix and the real bug it fixed, the pure reduced-motion functions, the generated (not sourced) icons and the copyright-avoidance reasoning behind their specific design, the new `testBankWord` fixture parameter and why it is additive rather than a change to `testWord`/`testWords`, and the PWA status block's never-steals-focus and never-downgrades-an-already-achieved-readiness design. No PRD value, gameplay rule or accepted M1–M4 behavior changed.

### Manual demonstration

Demonstrated against real built production output in headless Chromium via Playwright (no GUI browser or physical device was available in this session — see the device matrix above for what remains for the owner to run):

1. A fresh profile visits the production build, the title screen's offline-readiness line settles to "Ready to play offline after this visit," and `CacheStorage` is confirmed to actually hold the shell, a JS chunk, an icon and the manifest (`e2e/pwa.spec.ts`).
2. With the network then cut off entirely (`context.setOffline(true)`, which fails any request the worker does not answer from cache — not a mock), both reloading that tab and opening a brand-new tab in the same profile load the title screen and play a real round, with score rising through real movement (`e2e/pwa.spec.ts`).
3. A second, genuinely different production build (a real second `vite build`, not an edited copy) is deployed to the same origin/scope while one tab has an active run and a second sits at TITLE; both are provably undisturbed (score keeps rising in the first, the second still shows the old build's heading) while the new version installs in the background and becomes `waiting`; closing both tabs and opening a third shows the new build's content, controlled by the new worker, and still works offline after a further disconnect (`e2e/pwa-update.spec.ts`).
4. A build with one precached icon deleted (simulating a failed deploy) is then served at the same origin; the update attempt never reaches `waiting`, and the previously active (second) build keeps working exactly as before (`e2e/pwa-update.spec.ts`, same run).
5. `prefers-reduced-motion` is toggled both at load and at runtime via `page.emulateMedia`; the ball's on-canvas fill colour, sampled directly from pixels, holds perfectly steady in reduced motion across a full 2,000 ms D015 cycle and resumes varying the instant the preference is switched back, with no maze state advancing from the toggle itself (`e2e/reducedMotion.spec.ts`).
6. `localStorage.getItem` is made to throw, and separately `setItem` is made to throw a quota-exceeded error, each installed before any page script runs; in both cases a real round starts and scores, the in-memory best survives a trip through the pause screen back to title, and a reload returns cleanly to title with no thrown error (`e2e/storage.spec.ts`).
7. An eight-letter word (`COMPUTER`) and its real category (`Technology`) are guessed with four genuine misses first, then solved, reaching the level-complete panel with that content, then all four remaining levels (same pinned word, so each catches and solves immediately) reach campaign-complete — which this run's real, unscripted scoring happened to push past 10,000 points, also exercising the earned-life badge live rather than only through the separate synthetic HUD-text check. Both a 640 × 360 landscape viewport and this long-content journey stay within the viewport with no horizontal overflow (`e2e/layout-content.spec.ts`; screenshots in `docs/evidence/m5/`).

This demonstration is automated, against real built output and, for the PWA journeys, real worker/cache/update behavior — not mocked — but it is not a claim that a human has installed or played this on an actual device.

### Limitations

- **Real-device install, offline and touch/orientation checks are UNVERIFIED** (AC2/AC4/AC9): no physical Android, iOS or desktop device, and no GUI browser or HTTPS-hosted deployment, were available in this headless session. See the device matrix above for the exact owner-run steps to resume this.
- No human playtest for M5's layout/focus/reduced-motion changes; automated journeys demonstrate the mechanics and the absence of overflow, not that the experience feels right on a real phone.
- Real OS-level text scaling/zoom (as opposed to viewport-size changes) was not exercised.
- Only Node 26.7.0 was available in this session, so the advertised minimum of 22.12.0 was **not** re-verified for this milestone; the declared engine range and the lockfile are unchanged (no new npm dependency was added for the service worker, the icons or reduced motion).
- The generated app icon reuses this project's own established player-disc/ball-ring shapes in a rotated, ringed combination chosen to read as distinct from the plain arcade mark (see D021); the owner should still review it, and may want different artwork, before any public or commercial use.
- Reaching a five-digit score and the earned-life badge for the dedicated HUD-overflow check (item AC1) uses direct `textContent` assignment after stubbing `requestAnimationFrame`, rather than extended real play, for the same reason M4's handoff recorded the 10,000-point threshold as impractical to reach live within one session; item 7 of the manual demonstration above does reach it through entirely real play as an incidental bonus.
- M6 remains responsible for: the remaining fruit-edge regression expansion and three full human release-run playtests carried forward from M4/M5 planning (D020); a settings/preferences screen was not added, since none of M5's scope needed one beyond the OS reduced-motion preference.
- Claude's elapsed work for this milestone was a single long session; active session time and token usage were not measured and are not estimated here, consistent with prior milestones' honest gaps in that reporting.

## Review round 1 — Codex

Pending. Record reviewed SHAs, independent versus reported checks, criteria assessment, stable finding IDs with severity/file/line/impact/required behavior, outcome and next owner. M5 is not accepted yet.

## Fix response — Claude, if needed

Append finding-by-finding resolutions, new implementation SHA and verification. Preserve review history.

## Acceptance — Codex

Pending. Record accepted revision, verified criteria, remaining limitations and STATUS update before M6.
