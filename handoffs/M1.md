# M1 — Playable maze

## Brief — Codex

- State: ACCEPTED (Codex re-review completed 2026-09-17; see review round 2 and acceptance below).
- Next owner: Codex; prepare the M2 brief. Carry the nonblocking R3 metadata cleanup into Claude's next handoff.
- Prepared: 2026-09-17.
- Product baseline: PRD v1.0, especially sections 2, 3 (movement and dots), 4 (simulation boundaries), 5, 6, 7 (M1), and 8.
- Layout references: `WIREFRAMES.md`, screens 01, 03, and 05. These include later-milestone features; implement only the M1 subset below.
- Repository: `/Users/bhj/Documents/bhj-ai-hackathon`.
- Observed branch: `main`.
- Reference base commit: `cebf8aadacb4e52d4bd54b5476b93323bf74f663` (shared workflow documentation; no application code).
- Dependencies: M0 planning complete. No application, package manifest, lockfile, test scripts, or production build exists yet.
- Estimate: PRD allocation of 1.5–2 focused hours. Record actual time and flag overruns; this is not a deadline to skip required checks.

### Starting checkpoint

At preparation time, wireframes and updates to PRD, README, status, and decisions are uncommitted. This brief adds another planning change. These files are shared input, not unrelated changes to discard. Before implementation, inspect current HEAD and working-tree changes. If the owner has already committed this planning work, record that commit below as the implementation base. Otherwise, preserve and checkpoint the known planning files separately from implementation, then record the resulting full SHA. Do not include unfamiliar changes without resolving their ownership.

- Implementation base SHA: `f99531c80f0bf8abf542c95f8b108da157bcab49`. At start of implementation the working tree was clean and all planning work (including this brief and D007) was already committed by the project owner, so no separate planning checkpoint was needed.
- Working branch: `main` (no parallel checkout work is planned).

### Objective and exit demonstration

A fresh checkout can install dependencies, start the app, display a title screen, and enter a playable maze. A player can navigate with keyboard or touch, queue turns, traverse side tunnels, and collect dots without crossing walls or collecting the same dot twice. Clearing all dots does not finish the level. This is the functional baseline for M2, not yet the complete Hac-Man game.

### In scope

1. **Project foundation:** TypeScript, Vite, Canvas 2D maze rendering, HTML/CSS menus/HUD/controls, Vitest, and a minimal Playwright browser smoke suite. Pin compatible dependencies in a lockfile and document a tested runtime version. Use npm for this initial scaffold unless the repository gains an existing package-manager convention before starting.
2. **Title and instructions:** title, a working Start action, and concise keyboard/touch instructions. Start enters a fresh level-one maze with score zero. No fake playable features or static success screens.
3. **Original maze:** one authored tile map with walls, connected player corridors, a valid player spawn, dots, and paired left/right tunnel exits. The wireframe maze is illustrative; do not copy it as unvalidated level data. Reserve a central enemy-home region for M3 if useful, but keep it inaccessible to the player and implement no enemies yet.
4. **Player movement:** continuous cardinal movement, no diagonal movement, buffered turns at legal junctions, stopping at walls, and tunnel wraparound. Begin stationary until the first direction. Once moving, releasing a key/button does not stop the player; the player continues until a wall or another legal direction. Keep one latest requested direction; an illegal requested turn waits while the current legal movement continues. Allow a reversal in a corridor without wall penetration. Keep speed configurable.
5. **Dots and score:** consume each dot once for 10 points; update visible score. A dot-free maze remains navigable without a level-complete transition. Do not spawn a collectible under an inaccessible tile.
6. **Input:** arrow keys/WASD and a visible directional pad using pointer/touch input. A visible pad is the required M1 mobile path; swipe may be added later. Prevent scrolling for handled movement keys only when the game owns input, and suppress touch gestures only on gameplay controls. Clear stale queued/held input when leaving the play context.
7. **Responsive shell:** keep the maze, score, level/mode label, and directional pad together at 360 × 640 CSS pixels. Use a side panel for desktop hints as appropriate; preserve tile aspect ratio. Touch buttons are at least 44 × 44 CSS pixels. Give menu/control buttons labels and visible focus. A simple original player shape is sufficient.
8. **Deterministic engine:** separate maze/state/movement/dot logic from canvas, DOM, input events, and animation scheduling. Use a fixed simulation step with bounded catch-up; background delays must not teleport the player or replay minutes of movement. Make the simulation directly step-able in tests. Avoid building unused systems for later milestones.
9. **Working documentation:** replace README's “no application” notice when true; document only commands actually established and verified. Describe this milestone's supported behavior and remaining game features. Preserve the accepted project estimates and existing license.

Suggested module boundaries (names are illustrative): maze data/validation, game state and simulation step, movement, input adapters, canvas renderer, application shell, unit/integration tests, and browser smoke tests. Claude may choose a simpler file structure if these boundaries remain testable.

### Out of scope

Ball movement/capture, word selection, guessing UI and level completion (M2); enemies, power pellets, lives/death, full pause/resume and restart flows (M3); fruit, full scoring, extra lives, campaign, high score, persistence and sound (M4); PWA installation/cache/update behavior and final device polish (M5); release cleanup and complete final documentation (M6). Do not render inactive mute, guessing, enemy, or result controls merely to match the full wireframe. Word and lives HUD fields can wait for the corresponding behavior.

A full pause interface is not required in M1. Implement input cleanup and safe timing on focus loss now; document the current behavior, with explicit pause/resume semantics completed in M3. Public hosting, account setup, and deployment are not part of this milestone.

### Acceptance criteria

| ID | Observable criterion | Required evidence |
| --- | --- | --- |
| AC1 | Documented clean installation, dev server, type check, tests, production build, and preview work. Start opens an actual playable maze. | Exact commands/results; browser smoke and manual launch. |
| AC2 | Tile map is rectangular, uses known tile types, has a valid spawn and matched tunnel endpoints, and every player corridor/dot is reachable from spawn using legal edges. | Map validation and reachability unit tests on the actual map; at least one invalid-map fixture rejected. |
| AC3 | Movement cannot cross walls, turn through corners, or leave bounds except at tunnel endpoints. A buffered legal turn executes at the first eligible junction; reversal works in a corridor. | Deterministic unit tests for wall stop, queued turn, illegal turn, reversal, and both tunnel directions. |
| AC4 | Dots disappear on traversal and award exactly 10 points once. Traversing a consumed dot does not rescore it. Clearing the last dot leaves gameplay active. | Simulation integration tests and visible score change through real controls. |
| AC5 | Keyboard and visible touch pad drive the same movement rules. No diagonal/stale-input behavior, page scrolling from active arrow controls, or movement input on the title screen. | Input integration coverage and browser journeys using keyboard and touch/pointer controls. |
| AC6 | With the same simulated elapsed time and input schedule, different render cadences produce equivalent movement within numerical tolerance. A long frame cannot pass through a wall. | Fixed-step/movement tests, including a delayed-frame case. |
| AC7 | At 360 × 640 and a representative desktop viewport (e.g. 1280 × 800), layout has no horizontal overflow; maze and mobile pad are visible together; tiles remain square and controls meet minimum size. | Browser screenshots and geometry assertions; manual visual inspection of the running UI. |
| AC8 | Application logic is testable without canvas/DOM; README accurately describes M1 commands and limitations, and the handoff identifies the reviewed revision. | Code inspection; completed implementation handoff; README and committed checkpoint. |

### Verification contract

No commands below exist yet. Establish this intended interface in the scaffold, then run it and record actual outcomes:

| Intended command | Purpose |
| --- | --- |
| `npm ci` | Reproduce dependency installation from the committed lockfile. Use initial installation to create the lockfile first. |
| `npm run dev` | Local development server. |
| `npm run typecheck` | TypeScript checking without emission. |
| `npm test` | Unit and simulation/input integration tests, once and non-interactively. |
| `npm run test:e2e` | Playwright smoke journeys and viewport checks; document browser installation prerequisites. |
| `npm run build` | Production output. |
| `npm run preview` | Serve and manually inspect the production build. |

Tests should prove behavior rather than mirror implementation. Use deterministic simulation fixtures for edge cases and at least one real-controls browser path for collection; do not pass browser smoke tests by mutating player position or score directly. A narrow test-only readout of game state is acceptable for assertions if it is isolated from the production UI. Avoid timing-flaky assertions that assume an exact rendering frame.

Manual exit demonstration:

1. Launch the production preview and use the keyboard to select Start.
2. Navigate corridors with arrows/WASD; demonstrate a queued turn and wall stop.
3. Traverse each direction of the side tunnel without an out-of-bounds position.
4. Collect dots and revisit a cleared corridor; show the score is unchanged on revisiting.
5. At 360 × 640, use the visible pad to navigate and confirm the maze/control layout fits.
6. Inspect the console for errors and record viewport, browser, screenshots, and any limitations.

Browser emulation is sufficient for this milestone's mobile smoke check, but label it as emulation. It does not complete M5's real-device requirements. The project owner visually verified and accepted the wireframes on 2026-09-17 (D007). Claude must still visually inspect the implemented layout; approval of the planning sheets does not establish a passing runtime UI.

### Completion and handoff

Mark IMPLEMENTING in this file and `STATUS.md` when beginning. Work only on M1. When its criteria pass, create an implementation checkpoint, complete the section below, mark REVIEW, and transfer ownership to Codex. Do not proceed to M2 or self-accept the milestone. If blocked, state the exact blocker and next action; record unavailable checks as unverified rather than passed.

## Implementation handoff — Claude

- Implementation base SHA and branch: `f99531c80f0bf8abf542c95f8b108da157bcab49` on `main`. The working tree was clean at start; no planning checkpoint was needed.
- Implementation commit SHA: `86c5f10a91ad0a79e34b5d99ca45c6ac4f010775` on `main` (this record is the following documentation-only commit).
- Summary of changes:
  - Scaffolded the application: `package.json` with pinned exact versions, `package-lock.json`, `tsconfig.json`/`tsconfig.build.json`, `vite.config.ts`, `vitest.config.ts`, `playwright.config.ts`, `index.html`, and `.gitignore` entries for build and test output.
  - `src/game/` holds the browser-independent simulation: `maze.ts` (tile model, layout validation, legal moves, reachability), `mazeData.ts` (the authored 21 × 23 level-one layout), `actor.ts` (movement, buffered turns, wall stops, tunnel wrapping), `game.ts` (status, score, dots, fixed step), `loop.ts` (fixed-step accumulator with bounded catch-up), and `config.ts` (speed, step, catch-up bound, dot value).
  - `src/input/inputRouter.ts` maps arrow keys, WASD, and pad presses onto the same game requests and reports whether the game consumed a key. `src/render/renderer.ts` draws the maze, dots, and player on a canvas with integer square tiles. `src/app/shell.ts` wires DOM events, resizing, the animation frame, focus-loss cleanup, and a read-only test readout. `src/styles.css` and `index.html` provide the HUD, stage, side panel, directional pad, and title screen.
  - Tests: `tests/` (46 Vitest cases across maze, movement, game, loop, and input) and `e2e/` (7 Playwright journeys run on a desktop and an emulated mobile viewport, 14 runs in total).
  - Documentation: README setup/commands/limitations, decision `D008`, and this handoff. Screenshots in `docs/evidence/m1/`.
- Acceptance evidence (AC1–AC8):

| ID | Evidence |
| --- | --- |
| AC1 | Clean `rm -rf node_modules dist && npm ci`, then `npm run typecheck`, `npm test`, `npm run build`, `npm run test:e2e`, `npm run dev` (HTTP 200) and `npm run preview` (HTTP 200) all succeeded; outcomes below. Start opens a real maze: `e2e/gameplay.spec.ts` “keyboard journey…” asserts mode Chase, movement, and score through real controls. |
| AC2 | `tests/maze.test.ts`: rectangularity and known tile types; walkable spawn and matched tunnel endpoints; every corridor and dot reachable from spawn; reserved enemy home unreachable and dot-free; no walkable non-tunnel border. Invalid fixtures rejected: non-rectangular, unknown character, zero/two spawns, unreachable pocket, unmatched tunnel endpoint, walkable border, and `MazeValidationError` on creation. |
| AC3 | `tests/movement.test.ts`: wall stop; no wall crossing with a single 500-tile step; illegal direction kept queued without moving; stationary until first input; buffered turn taken at the first legal junction; no corner cutting before the junction centre; latest-request-only; mid-corridor and centre reversal; both tunnel directions with in-bounds positions; equal distance across the tunnel seam. Browser: “walls stop the player and a queued turn…” and “the side tunnel wraps the player…”. |
| AC4 | `tests/game.test.ts`: 10 points per dot, exactly once; retracing a cleared corridor adds nothing; the maze stays in Chase with zero dots remaining and is still navigable; a dot is collected only at its tile centre; the authored spawn tile holds no dot. Browser: score rises through real key presses and is unchanged when retracing the cleared corridor. |
| AC5 | `tests/input.test.ts`: arrow/WASD mapping, case-insensitive, non-movement keys ignored; movement keys not consumed on the title screen; pad and keyboard produce identical snapshots for all four directions; latest-request-only prevents diagonal behavior. Browser: title-screen keys leave status/score untouched and are not default-prevented, in-play movement keys are default-prevented with `scrollY === 0`, and the pad is driven by a real touch tap on the mobile project. |
| AC6 | `tests/loop.test.ts`: 120 Hz, 60 Hz, and 20 Hz cadences over the same simulated time and input schedule agree on position, direction, and score to 1e-6; whole-step accounting with remainder carry; catch-up bounded to 12 steps for a 60 s stall; repeated 30 s stalled frames never move the player past the wall. `tests/movement.test.ts` covers the single long step. |
| AC7 | `e2e/layout.spec.ts` on desktop 1280 × 800 and emulated mobile 360 × 640: no horizontal overflow, maze and pad both inside the viewport, canvas tile width equals tile height, and all four pad buttons at least 44 × 44 CSS pixels. Measured: desktop canvas 672 × 736 (32 px tiles), mobile canvas 336 × 368 (16 px tiles, pad bottom 632 ≤ 640). Screenshots in `docs/evidence/m1/`, visually inspected. |
| AC8 | `src/game/` imports nothing from the DOM, canvas, or timers, and every Vitest suite runs in the `node` environment. README documents the verified commands, current behavior, and limitations; this handoff records the reviewed revision. |

- Exact commands, outcomes, and failures:

| Command | Outcome |
| --- | --- |
| `rm -rf node_modules dist && npm ci` | Success; 43 packages, 0 vulnerabilities. npm warns that `fsevents@2.3.3` has install scripts not covered by `allowScripts` (transitive, optional). |
| `npx playwright install chromium` | Success; Chrome Headless Shell 153.0.8010.12 (chromium-headless-shell v1243). Required once before `npm run test:e2e`. |
| `npm run typecheck` | Success; no diagnostics (TypeScript 7.0.2). |
| `npm test` | Success; 5 files, 46 tests passed. |
| `npm run build` | Success; `dist/index.html` 2.92 kB, CSS 3.31 kB, JS 11.13 kB. |
| `npm run test:e2e` | Success; 14 passed (7 journeys × desktop and mobile projects) against the production build. |
| `npm run dev` | Success; HTTP 200 at <http://127.0.0.1:5173>. |
| `npm run preview` | Success; HTTP 200 at <http://127.0.0.1:4173>. |

  No failing checks remain. Two issues found and fixed during implementation: Vite's preview server bound to IPv6 only, so both servers now bind `127.0.0.1`; wall tiles first rendered as disconnected blobs and now fill their tile with outlines only where they meet a corridor.

- Manual demonstration, screenshots, browser/device or emulation details: driven against `npm run preview` (production build) with Playwright Chromium 153.0.8010.12 on macOS 24.6.0, at desktop 1280 × 800 and emulated mobile 360 × 640 (Pixel 5 descriptor, touch enabled). Observed, in order: the Start button holds initial focus and Enter starts Chase; running left stops at column 7 against the wall at column 6 and stays there (score 30); pressing up then left while at row 12.3 keeps `pending=left` until the tunnel row, where the turn is taken; travelling left through the tunnel re-enters at the right edge (x = 13, row 10, always 0 ≤ x < 21); travelling right re-enters at the left edge (x = 7); retracing the cleared corridor leaves the score at 190; on mobile, tapping the pad queues a turn that is taken at the next legal junction (score 190 → 250) and the layout reports canvas 336 × 368, pad bottom 632 ≤ 640, no horizontal overflow, `scrollY === 0`; no console or page errors in either run. Committed screenshots: `docs/evidence/m1/{desktop,mobile}-{title,chase}.png`, captured by `e2e/layout.spec.ts` and visually inspected.
- Decision IDs, deviations, and known limitations: `D008` records the implementation choices. No scope deviation. Limitations: mobile evidence is browser emulation only (M5 owns real devices); there is no pause interface, though input is cleared and the simulation stops while the page is hidden; renderer and DOM shell have no unit tests and are covered by browser journeys instead; the enemy home is drawn but empty until M3.
- Unverified checks: real-device touch, installation, offline behavior, and PWA checks (M5 scope, not attempted); performance on a representative phone; browsers other than Chromium; screen-reader behavior beyond labels, roles, and focus handling.
- Working-tree changes outside the implementation commit: none expected; `dist/`, `node_modules/`, `test-results/`, and `playwright-report/` are ignored.
- Focused work, Claude active session time, and token usage: approximately 0.6 h of Claude session time on 2026-09-17, within the 1.5–2 h PRD allocation for M1. Token usage is not exposed to this session; estimated 0.25–0.45 million cumulative tokens, labelled estimated rather than measured.
- Next action: Codex reviews the diff from `f99531c8` to the implementation commit against AC1–AC8 and independently runs the commands above.

## Review round 1 — Codex

Completed 2026-09-17. Outcome: **CHANGES_REQUESTED**.

- Reviewed base: `f99531c80f0bf8abf542c95f8b108da157bcab49`.
- Reviewed implementation: `86c5f10a91ad0a79e34b5d99ca45c6ac4f010775`; checkout HEAD `e31de3a8fad6635c4d3e25d1546a16b02ff5891d` adds the documentation handoff only.
- Independent checks on Node 26.7.0: `npm ci` passed (optional fsevents install-script warning); `npm run typecheck` passed; `npm test` passed (46 tests); `npm run build` passed; `npm run test:e2e` passed (14 browser runs). Browser tests required approved execution outside the sandbox because localhost binding was initially denied. No failing automated checks remain in the existing suite.
- Independently started `npm run preview -- --port 4174 --strictPort`, opened the production app, started play, and measured viewport/canvas/pad geometry while resizing. Visually inspected the mobile Chase evidence image. The browser suite exercised the real keyboard/touch collection paths; it does not cover the failing resize sequence below.
- Claude-reported checks not independently repeated: development-server smoke, the entire manual demonstration sequence, minimum-version Node execution, and all real-device checks. No new runtime-support claim is made for Node 20.
- Acceptance assessment: AC2–AC6 have passing existing automated coverage and no additional blocking finding from this review. AC7 fails the desktop resize case below. AC1/AC8 need the runtime support documentation/package metadata corrected. The passing tests are evidence for their scenarios, not blanket acceptance of M1.

### R1 — P2: Fit the maze after reducing viewport height

- Status: RESOLVED in `1a60082863700f816a02a0affe94f805efd570c1`; see the fix response below. Originally OPEN, blocking M1 acceptance (AC7).
- Location: `src/styles.css:29–37` (`#app` uses only `min-height`), with canvas sizing in `src/app/shell.ts:45–47`.
- Reproduction: start the production app, set viewport to 1280 × 1000, then reduce it to 1280 × 800 without reloading. Canvas remains 861 × 943 CSS pixels with bottom approximately 990.5; document height is 998 while the viewport is 800. The bottom of the maze is outside view during active play. A fresh 1280 × 800 load fits, which is why the current browser test passes.
- Cause: the outer flex layout can grow to its existing canvas content height, so the ResizeObserver receives a stage height constrained by that same canvas instead of the smaller available viewport. The canvas cannot shrink to fit the new height.
- Required change: make available maze height respond to viewport height and reserved HUD/controls space, preserving square tiles and usable controls. Add a browser regression that resizes from tall to short without reload and asserts the canvas bottom as well as the pad bottom is within the viewport. Preserve the passing 360 × 640 layout. Do not solve this by clipping the maze or disabling page overflow.

### R2 — P2: Align supported Node versions with the pinned toolchain

- Status: RESOLVED in `1a60082863700f816a02a0affe94f805efd570c1`; see the fix response below. Originally OPEN, blocking M1 acceptance (AC1/AC8).
- Location: `package.json:8–10` and README Requirements.
- Evidence: the app declares `node >=20.19.0` and README recommends that minimum, but installed/pinned Vitest 5.0.1 declares `^22.12.0 || ^24.0.0 || >=26.0.0`; Vite declares `^20.19.0 || >=22.12.0`. The application range therefore includes unsupported runtimes, notably Node 20 and early Node 22. With engine enforcement enabled, a clean dependency installation on those advertised versions is rejected.
- Required change: either narrow the declared/documented runtime support to a range compatible with all pinned tools and verify the advertised minimum, or select and verify a toolchain supporting the current promise. Keep package/lockfile metadata and README consistent. Node 26.7.0 is independently verified; other versions were not executed in this review.

- Next owner/action: Claude addresses R1/R2, adds the resize regression, reruns relevant checks, records resolutions and a new implementation SHA, then returns to REVIEW. M2 remains NOT_STARTED.
- Review time and token usage: not measured; unavailable. No budget change.

## Fix response — Claude, review round 1

Completed 2026-09-17. Returning to REVIEW.

- Reviewed base for this round: `86c5f10a91ad0a79e34b5d99ca45c6ac4f010775` (the revision Codex reviewed).
- New implementation commit SHA: `1a60082863700f816a02a0affe94f805efd570c1` on `main`. Its parent `5a1bfc8` is a documentation-only commit that checkpoints Codex's review round 1 write-up, which was uncommitted in the working tree; committing it first keeps the fix diff separable. This fix-response record is the following documentation-only commit.
- Decision record: `D009`.

### R1 — Fit the maze after reducing viewport height

- Status: RESOLVED.
- Reproduced first, on `86c5f10`: starting at 1280 × 1000 and shrinking to 1280 × 800 without a reload left the canvas 861 × 943 with bottom 1014 in an 800 px viewport, exactly as reported.
- Cause confirmed as described: `#app` had only `min-height: 100dvh`, so the column could grow to the canvas it already contained. The stage then reported that same height to the `ResizeObserver`, and the canvas it had produced kept it there. `.stage` also had an explicit `min-height: 140px` on narrow viewports and an automatic minimum width equal to the canvas, so neither axis was free to shrink.
- Change (`src/styles.css`): `#app` now occupies exactly the viewport box, `height: 100dvh` with a `100vh` fallback, instead of a minimum. `.stage` gets `min-width: 0` and `min-height: 0` on every viewport, replacing the 140 px floor and the desktop-only override that previously set `min-height: 0`. No renderer, shell, or markup change was needed: `MazeRenderer.resize` already fits an integer square tile into the space it is given, and the defect was the space it was given. Nothing is clipped and page overflow is not hidden; the document now fits the viewport.
- Regression added (`e2e/layout.spec.ts`, “reducing the viewport height refits the maze without a reload”): starts play, enlarges to the project viewport plus 240 px, shrinks back without a reload, then asserts the canvas bottom, the pad bottom, `document.documentElement.scrollHeight` and `scrollWidth` are all within the viewport, tiles are still square, the canvas is still usable, and every pad button is at least 44 × 44. It runs on both projects. Verified failing on the pre-fix code (desktop: expected ≤ 801, received 1014) and passing after the change.
- Additional manual geometry check against the production build, resizing without reload: 1280 × 1000 → canvas 861 × 943, bottom 991.5; → 1280 × 800 → canvas 672 × 736, bottom 788.0; → 1280 × 520 → canvas 420 × 460, bottom 510.0; → back to 1280 × 1000 → canvas 861 × 943, so the maze grows again as well as shrinking; → 360 × 640 → canvas 336 × 368, pad bottom 632. Document height equalled viewport height and tiles stayed square at every step.
- Unchanged: the fresh-load layouts. `docs/evidence/m1/desktop-chase.png` and `mobile-chase.png` are byte-identical to the accepted pre-fix captures, and the new `*-resized.png` captures are byte-identical to them in turn, which is the point — resizing down now lands on the same layout as a correct fresh load.

### R2 — Align supported Node versions with the pinned toolchain

- Status: RESOLVED.
- Declared engine ranges of the pinned tools: Vitest 5.0.1 `^22.12.0 || ^24.0.0 || >=26.0.0` (narrowest), Vite 8.3.0 `^20.19.0 || >=22.12.0`, Playwright 1.63.0 `>=20`, TypeScript 7.0.2 `>=16.20.0`. Their intersection is `^22.12.0 || ^24.0.0 || >=26.0.0`.
- Change: `package.json` declares that intersection in place of `>=20.19.0`; a new `.npmrc` sets `engine-strict=true` so an unsupported runtime is rejected at `npm ci` instead of failing later inside a tool; README Requirements states the range, names Vitest 5 as the binding constraint, lists the unsupported majors (20, 21, 23, 25), and records the verified versions. No dependency version changed, so `package-lock.json` is unchanged.
- Advertised minimum verified, not just declared. On Node 22.12.0 (npm 10.9.0), from a clean `rm -rf node_modules dist`: `npm ci`, `npm run typecheck`, `npm test` (46 passed), `npm run build`, and `npm run test:e2e` (16 passed) all succeeded. The same full sequence succeeded on Node 24.13.0 and on Node 26.7.0 (npm 11.19.0).
- The previously advertised minimum is now refused as expected: on Node 20.19.0, `npm ci` fails with `EBADENGINE … Required: {"node":"^22.12.0 || ^24.0.0 || >=26.0.0"} Actual: {"npm":"10.8.2","node":"v20.19.0"}`, rather than installing and breaking inside Vitest.
- Node binaries for 20.19.0, 22.12.0 and 24.13.0 were downloaded from nodejs.org into a scratch directory for these runs only; nothing outside the repository was installed or changed, and no version manager is required to reproduce them.

### Checks rerun at `1a60082`

Clean working tree, macOS 24.6.0 arm64, Playwright Chromium 153.0.8010.12.

| Command | Node 22.12.0 | Node 24.13.0 | Node 26.7.0 |
| --- | --- | --- | --- |
| `rm -rf node_modules dist && npm ci` | Success, 45 packages (npm 10.9.0) | Success | Success, 46 packages audited; the known optional `fsevents@2.3.3` install-script warning remains |
| `npm run typecheck` | Success, no diagnostics | Success | Success, no diagnostics |
| `npm test` | 5 files, 46 tests passed | 46 passed | 5 files, 46 tests passed |
| `npm run build` | Success | Success | Success; `dist/index.html` 2.92 kB, CSS 3.31 kB, JS 11.13 kB |
| `npm run test:e2e` | 16 passed | 16 passed | 16 passed (8 journeys × desktop and mobile) |
| `npm run dev` / `npm run preview` | not repeated | not repeated | HTTP 200 at <http://127.0.0.1:5173> and <http://127.0.0.1:4173> |

Browser runs rose from 14 to 16 because of the single new resize journey. Unit test count is unchanged at 46: R1 is a layout defect with no simulation behavior to cover, and the renderer/DOM shell remain covered by browser journeys rather than unit tests.

- Affected acceptance criteria rechecked: AC7 now passes both the fresh-load and the resize-without-reload cases on desktop and emulated mobile; AC1/AC8 now advertise a runtime that installs and runs, verified at the advertised minimum. AC2–AC6 are unaffected by these changes and their suites still pass.
- Remaining limitations, unchanged from the implementation handoff: mobile evidence is browser emulation only (M5 owns real devices); there is no pause interface; the renderer and DOM shell have no unit tests; the enemy home is drawn but empty until M3. New to this round: only macOS arm64 was exercised, and within the declared range only 22.12.0, 24.13.0 and 26.7.0 were run, so the `^24.0.0` and `>=26.0.0` lines are verified by one version each rather than at their lower bounds.
- Claude session time for this round: approximately 0.4 h on 2026-09-17. Token usage is not exposed to this session; estimated 0.10–0.20 million cumulative tokens for the round, labelled estimated rather than measured.
- Next action: Codex re-reviews `86c5f10` → `1a60082` against R1 and R2 and the affected criteria, and rechecks for regressions.

## Acceptance — Codex

Accepted on 2026-09-17 at implementation SHA `1a60082863700f816a02a0affe94f805efd570c1` after the independent recheck recorded below. AC1–AC8 are satisfied for M1's scope based on the combined review evidence. Real-device/PWA checks remain later milestones. R3 is a nonblocking metadata follow-up; it does not prevent a clean install or test/build execution on the verified supported runtimes.

## Review round 2 — Codex

- Date: 2026-09-17.
- Compared implementation revisions: `86c5f10a91ad0a79e34b5d99ca45c6ac4f010775` → `1a60082863700f816a02a0affe94f805efd570c1`. Checkout HEAD `1409a5bcbbc34484c85f73b5578e472ab8c8578f` adds the fix handoff only. Working tree was clean before verification.
- Independent checks on Node 26.7.0: `npm ci`, `npm run typecheck`, `npm test` (46 passed), `npm run build`, and `npm run test:e2e` (16 passed, including both new resize regressions). Browser tests ran with approved localhost access against a newly built production preview.
- Independent advertised-minimum checks: used the existing scratch Node 22.12.0 binary; confirmed its version, then ran `npm ci`, `npm run typecheck`, `npm test` (46 passed), and `npm run build`, all passing.
- R1: CLOSED. The CSS bounds the shell to viewport height and allows the stage to shrink. The added regression checks canvas/pad bounds, page dimensions, square tiles, and touch target sizes after a tall-to-short resize without reloading. Both desktop and mobile projects pass independently.
- R2: CLOSED for the runtime-support defect. Package engine range, strict installation policy, and README now match the pinned toolchain. The advertised minimum installs, type-checks, tests, and builds independently. Root lockfile metadata cleanup remains R3 below.
- Claude-reported evidence not independently repeated this round: Node 24.13.0 runs, Node 20 rejection, browser tests on Node 22.12.0, and the additional manual viewport sequence. These remain attributed to Claude, not Codex. Real-device and PWA validation remains outside M1.
- Outcome: ACCEPTED with one nonblocking follow-up. No new blocking regression found in the fix diff.
- Next action: Codex prepares M2; Claude includes R3 in the next implementation checkpoint.
- Review time/token usage: unavailable; no budget change.

### R3 — P3: Refresh root lockfile engine metadata

- Status: OPEN, nonblocking; carry into the next Claude handoff.
- Location: `package-lock.json:18–20`.
- The root `packages[""].engines.node` still reads `>=20.19.0`, while `package.json` now declares `^22.12.0 || ^24.0.0 || >=26.0.0`. Contrary to the fix response's explanation, unchanged dependency versions do not eliminate the need to refresh changed root metadata.
- Required cleanup: regenerate the lockfile metadata using the supported toolchain without upgrading dependency versions; verify its root engine range matches `package.json` and that `npm ci` still passes.
- Nonblocking rationale: independent clean installs/tests/builds pass on Node 22.12.0 and 26.7.0. This is a consistency issue, not an observed runtime failure.
