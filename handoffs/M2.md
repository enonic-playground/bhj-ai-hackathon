# M2 — Chase, catch, guess, solve

## Brief — Codex

- State: READY.
- Next owner: Claude; implementation has not started.
- Prepared: 2026-09-17.
- Product baseline: PRD v1.0, sections 3–6, M2 in section 7, and relevant tests in section 8.
- Layout references: `WIREFRAMES.md`, mobile/desktop Chase and Guessing (01–04), level-complete panel (06), and transition/accessibility notes. Implement the M2 subset; enemies, lives, and campaign progression remain later work.
- Repository/observed branch: `/Users/bhj/Documents/bhj-ai-hackathon`, `main`.
- Reference base commit: `1f8fa6032856c5746f0182e8cb8a6701e9feb7d6` (M1 acceptance documentation).
- Last accepted implementation: `1a60082863700f816a02a0affe94f805efd570c1`.
- Dependencies: M1 ACCEPTED. Its verified baseline includes 46 unit/integration tests and 16 browser runs.
- Estimate: PRD allocation of 2–2.5 focused hours. Record actual work and flag overruns without dropping checks or silently widening scope.

### Starting checkpoint and ownership

Read shared instructions and current status before editing. The working tree was clean before this brief was prepared. This brief and its coordination updates may still be uncommitted when Claude begins. Preserve/checkpoint known planning changes separately, or use the owner's planning commit if already present; record the actual base SHA before implementation. Do not discard unfamiliar changes or overwrite work in progress.

- Implementation base SHA: Claude records before code changes.
- Working branch: Claude records the actual branch.
- Codex reviews implementation; Claude writes code, tests, and implementation documentation. No autonomous agent dispatch or publishing is implied by READY.

### Objective and exit demonstration

Deliver a playable single-word round using the existing maze: Start → chase a moving ball → catch → correct guess stays in Guessing → wrong guess triggers a countdown and a new chase → catch again → solve the word → show a result and allow a fresh round. Preserve M1 movement, dots, scoring, controls, and resize behavior.

### In scope

1. **Ball movement and appearance.** Add one clearly distinguishable rolling target, moving at 80% of configured player speed. It follows player-walkable corridors and tunnels, never enters walls/home/door tiles, never consumes dots, and cannot stop indefinitely at a dead end. Choose a legal direction at tile centres; use seeded/injected randomness for tests. A reasonable initial policy is to exclude immediate reversal when another exit exists and reverse at dead ends. Document the chosen policy. Simulate movement independently of render cadence.
2. **Safe spawns.** On round start and after each wrong letter, select a random reachable corridor at least six shortest-path tiles from the player, using legal maze edges including tunnel links. Exclude the player's occupied tile, walls and home/door tiles. If the player is between centres, use a documented consistent graph anchor and also exclude adjacent occupied segment endpoints so a spawn cannot overlap the player. If no candidate meets the distance rule, select the farthest remaining reachable eligible cell, randomizing ties. Small test fixtures with no eligible cell must fail clearly or return a handled no-spawn result; never loop forever or spawn on the player. The production maze must always have eligible cells. A respawn may reuse a previous location if it still satisfies the rules.
3. **Capture and freeze.** Detect contact robustly as player and ball move, including opposing travel, delayed render frames, and the tunnel seam. Use swept contact or bounded substeps with a documented capture radius. Do not capture through a wall or compare distant non-tunnel edges as adjacent. Resolve player dots reached before capture, then enter GUESS exactly once and remove the ball. Freeze player position, direction, movement/maze animation, collectible state, and simulation timers throughout GUESS and LEVEL_COMPLETE. Preserve actor position/direction for later resumption; clear queued/held input on transitions.
4. **Word rules.** Select one categorized common English word of 4–8 ASCII letters at round start and retain it across chases. Include a small local seed list (for example 6–10 entries, including repeated-letter words) so the round is playable; the 50-word bank, no-repeat campaign selection, difficulty progression, and five-level campaign are M4. Inject word selection for tests. Show category, mask, and guessed-letter state in the HUD/panel. Accept only individual A–Z letters, case-insensitively. A correct guess reveals every occurrence and awards 100 points per newly revealed position; keep GUESS active unless complete. Duplicate or invalid input has no effect. Record each wrong letter and return to chasing without reducing score or inventing a life penalty.
5. **Resume transition.** Use a configurable two-second initial countdown after a wrong letter, showing the missed letter and “Catch the ball again.” Select the new ball spawn once when entering RESUMING; player and ball stay frozen until the countdown completes. Only the countdown advances in RESUMING. Ignore movement and letter requests during it, and clear input again on exit. Then return to CHASE from the preserved player position/direction. Process transitions centrally so rapid clicks, key repeat, and repeated update calls cannot trigger multiple respawns or duplicate bonuses.
6. **Round completion.** The final correct letter transitions once to LEVEL_COMPLETE, awards the 1,000-point word bonus once, and displays the solved word, word bonus, and total score. For this single-round milestone, offer “Play again” (fresh round, score/dots/actors/word/guesses reset) and “Title screen.” Do not expose an inactive Next level button or simulate the five-level campaign. A new round may choose the same word; campaign uniqueness arrives in M4. Clearing dots alone never completes a round and must not disable ball spawning/capture.
7. **Input and focus.** CHASE owns arrows/WASD and the movement pad; GUESS owns physical letter keys and the accessible HTML letter grid. Thus W/A/S/D guess letters in GUESS without moving the player. Keep shortcuts with Ctrl/Meta/Alt available to the browser. Suppress browser defaults for owned movement keys even when ignoring auto-repeat, while preventing held/repeated input from leaking into a newly entered mode. Ignore movement in GUESS and guesses in CHASE/RESUMING/results. Touch guesses and physical keys use identical rules. A pointer event must not also guess the same letter or activate a replacement control after a mode switch.
8. **Responsive guessing UI.** Use the accepted stacked mobile overlay and desktop sidebar. Keep score, mode, level, category, and word progress readable. Use at least 44 × 44 px letter buttons, disabled guessed letters with non-color-only hit/miss feedback, and visible focus. Keep all 26 letters accessible at 360 × 640 without invoking a native keyboard; preserve M1 tall-to-short resizing. Move focus into the guessing panel on capture and to a sensible enabled letter after a correct guess; announce word/guess feedback through a live region. On resumption, focus the play region; on completion, focus the result heading or primary action. Hidden panels must not retain interactive controls in the tab order. Do not add fake lives, mute, or pause controls.
9. **Background timing.** Preserve M1's safe background-time handling for the new states too: hidden time must not run down RESUMING or replay accumulated maze time on return. Clear input on focus loss and avoid advancing frozen modes via render callbacks. Full PAUSED state, explicit resume on focus return, and the pause menu remain M3, as staged in M1; do not claim those are implemented yet.
10. **R3 cleanup.** Regenerate root lockfile metadata so `packages[""].engines.node` matches `package.json` (`^22.12.0 || ^24.0.0 || >=26.0.0`). Do not upgrade dependencies to accomplish this. Verify `npm ci` and record closure of M1 R3 in the implementation handoff; Codex confirms it during review.

### Architecture and boundaries

Extend the existing simulation/state, input router, renderer, and DOM shell rather than replacing the working foundation. Keep ball spawning/movement, contact detection, letter evaluation, and transitions testable without browser APIs. Suggested additions are small ball, word, and spawn-rule modules; exact names are Claude's choice. Extend the state model with GUESS, RESUMING, and LEVEL_COMPLETE using the repository's existing naming convention.

Retain one simulation clock and central transition authority. Reuse the maze traversal rules for distance calculations and tunnels. Inject randomness; avoid nondeterministic tests and dependence on wall-clock sleeps for simulation assertions. Expose only a read-only snapshot for browser assertions, with masked word/guess state; use test fixtures or injected seeds rather than a production control that mutates game state or reveals the answer. Design a narrow test entry/configuration if needed and explain how it is excluded from ordinary production use.

Out of scope: enemies and their collision precedence, pellets, deaths/lives, full pause/restart UI (M3); complete word bank/campaign, difficulty, fruit, full arcade scoring, persistence and sound (M4); PWA and real-device release validation (M5). Letter/word scoring belongs here because it is part of the guessing outcome; do not implement the remaining scoring systems early. No backend, AI word service, account, deployment, or new framework is needed.

### Acceptance criteria

| ID | Observable criterion | Required evidence |
| --- | --- | --- |
| AC1 | R3 root lockfile engine range matches package metadata; supported clean install, typecheck, tests, production build and preview still work. | Lockfile diff with no dependency upgrades; command results on a supported runtime. |
| AC2 | One moving ball stays on legal paths, moves at 80% player speed, uses tunnels, reverses at dead ends, and consumes no dots. | Deterministic unit/integration tests and visible chase demonstration. |
| AC3 | Initial/respawn candidates satisfy reachability and six-tile path distance; fallback and no-candidate behavior are safe, including fractional player positions. | Seeded tests for normal selection, tunnel-shortened paths, farthest fallback, exclusions and empty eligible sets. |
| AC4 | Contact enters GUESS exactly once, removes the ball, and freezes simulation until a permitted transition. No missed crossing or through-wall captures. | Capture tests for same corridor, opposite directions, tunnel seam, wall separation and delayed render cadence; repeated step calls leave GUESS state unchanged. |
| AC5 | Correct letters reveal all occurrences, score only new positions, and keep guessing active. Invalid/duplicate guesses cannot change state/score or cause respawn. | Case-insensitive and repeated-letter unit tests; correct/wrong/duplicate sequences using real browser controls. |
| AC6 | A wrong letter persists, spawns one valid ball and enters the countdown; only the countdown advances, then Chase resumes with the same word/player/score/dots. | Integration snapshots before/after each state, including large deltas, repeated input, and hidden-time handling. |
| AC7 | Final letter produces exactly one completion/bonus even under repeated events. Play again starts a clean round and Title screen works. Dot clearance alone cannot complete a round. | State/score/reset integration tests and a complete browser solve/replay journey. |
| AC8 | Keyboard/touch inputs act only in the current mode; W/A/S/D guess in GUESS; held/repeat/pointer input does not cross mode boundaries. | Router/DOM integration and browser tests for mode changes, duplicates and focus. |
| AC9 | Guess panel and all letters are usable at 360 × 640 and desktop, targets are at least 44 × 44, visible focus and text feedback work, and resizing still fits the maze/pad. | Screenshots for Chase/Guess/countdown/result; geometry/focus assertions and manual inspection of the production build. |
| AC10 | Existing M1 movement/dot/resize behaviors remain covered; a deterministic real-controls journey catches, guesses correctly, misses, catches again, solves and replays. | Passing regression suite plus new browser journey; evidence maps tests/results to AC1–AC10. |

### Test strategy and commands

Use existing commands: `npm ci`, `npm run typecheck`, `npm test`, `npm run build`, `npm run test:e2e`, and `npm run preview`. Browser tests already build and preview production output; confirm they are reviewing the current build rather than reusing a stale server. Record runtime and any prerequisite browser installation. Repeat on the advertised Node minimum if changing toolchain requirements; R3 must not change the supported range.

Maintain the M1 tests' behavioral coverage. Some browser paths may now encounter the ball; make their starting conditions deterministic so a legitimate capture does not make movement assertions flaky. Do not remove assertions simply to make them pass. Add focused tests alongside each feature; no arbitrary minimum test count is required.

For unit/integration checks, inject small mazes, a fixed word such as APPLE, and a seeded random source. For browser checks, use a deterministic fixture with a reachable moving ball and real keyboard/touch controls: do not teleport the player, trigger capture via a hidden command, submit the answer by mutating state, or stub the core loop. At least one journey must exercise the actual capture algorithm and the complete loop. Use observable-state polling rather than timing-dependent delays for moving actors. Explain any test-only fixture route/configuration in the handoff.

Manual production demonstration:

1. Start a fresh round and identify the ball, category, and masked word.
2. Catch the ball; observe the dimmed/frozen maze and focus entering the letter panel.
3. Guess a correct letter (including a repeated letter where possible), then try a duplicate. Verify reveal/score and no duplicate effect.
4. Guess a wrong letter. Confirm feedback, frozen countdown, a valid ball respawn, and resumed chase with word progress intact.
5. Catch again and solve the word. Verify one completion/bonus and a clean replay.
6. Repeat the core interaction with touch controls at the mobile reference size; resize desktop from tall to short while checking both Chase and Guessing layouts.
7. Inspect console errors and focus order. Record browser/emulation/device, seed or fixture, screenshots and limitations. Real-device testing remains M5; label emulation honestly.

### Completion and handoff

Mark IMPLEMENTING in this file and `STATUS.md` when beginning. After verification, commit the implementation checkpoint, record its full SHA, complete the evidence below, mark REVIEW and transfer ownership to Codex. Do not self-accept M2 or begin M3. Record deviations and blockers explicitly. Update README's current behavior and remaining limitations without rewriting accepted estimates; record actual time/usage for later estimate calibration.

## Implementation handoff — Claude

Not started. Populate on delivery:

- Implementation base SHA / branch:
- Implementation commit SHA:
- Summary and touched modules:
- AC1–AC10 evidence (test names, commands, manual results):
- M1 R3 resolution and lockfile verification:
- Deterministic browser fixture/seed and production isolation:
- Exact checks, outcomes, failures and runtime:
- Manual demonstration and screenshots:
- Decisions/deviations, known limitations and unverified checks:
- Working-tree changes outside the implementation commit:
- Work/session time and token usage (measured/estimated/unavailable):
- Next action: Codex reviews this milestone's base-to-implementation diff.

## Review round 1 — Codex

Pending implementation. No M2 code or checks are accepted yet.

- Reviewed base and implementation SHAs:
- Independently run checks:
- Claude-reported checks not independently verified:
- AC1–AC10 assessment and M1 regression assessment:
- Findings (stable ID, severity, file/line, concrete impact and required behavior):
- Outcome, next owner/action, and review time/usage:

## Fix response — Claude, if needed

Append new implementation SHA, finding-by-finding resolutions, regression evidence and limitations. Preserve earlier review rounds.

## Acceptance — Codex

Pending. Record accepted SHA, supporting evidence, nonblocking follow-ups and the status update before moving to M3.
