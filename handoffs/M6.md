# M6 — Release quality

## Brief — Codex

- State: REVIEW (Claude → Codex, 2026-09-24). Implementation base `8514be507ace8bc6df0067a4ec06bc87b92c2574`; candidate `808be0f0d5d2a6cdd84a5de683fda311e797bfe9`.
- Owner: Codex (review of the committed candidate); Claude implemented.
- Prepared: 2026-09-24.
- Specification: PRD v1.0 sections 5–10, AGENTS.md, D015/D016, D023 and D024.
- Repository/branch: `/Users/bhj/Documents/bhj-ai-hackathon`, `main`.
- Reference base commit: `0d9b83e6cc62dcd9ac04fd3cbc835145b61f16ef` (clean at planning start).
- Dependencies: M5 ACCEPTED following owner-reported device validation on 2026-09-24; M5-R1–R6 closed. Accepted application revision: `b9e25940d0aaf3361fdcb52e4c88620e08b0df48`, with reviewed README correction `1f0bd0f6c31bc800c764a2e10b811b656918b140`.
- Objective: a reproducible, documented, demo-ready release satisfying PRD section 8, with explicit evidence for every release gate.
- Estimate: retain the PRD allowance of **1.5–2 focused hours**, using existing contingency if needed. Active session time is included, not added; record Claude/Codex time and token usage separately if available. No new measured usage or revised overall estimate is claimed.

### Starting checkpoint and boundaries

Checkpoint this brief and the accompanying planning updates before application work, or use the owner's planning commit; record that full SHA as the actual implementation base. Mark IMPLEMENTING when starting. Preserve unfamiliar changes and keep every increment runnable.

In scope: targeted remaining regressions, fixes for demonstrated release defects, limited cleanup, final README and evidence, clean-install verification. Expected areas are `tests/`, `e2e/`, README, release evidence and narrowly affected source/configuration. Review existing coverage before adding tests; do not duplicate it to increase counts.

Out of scope: the three recorded human playtests and demo rehearsal (owner decision D024), new features, audio/music/mute, new mazes or modes, backend/accounts/analytics, saved runs, broad refactors, unrelated dependency upgrades, changing accepted rules, public publishing, tagging a release or changing hosting settings. The existing Pages workflow deploys on pushes to `main`; do not push or deploy under this brief. Preserve the approved normal-motion feedback and reduced-motion behavior, exact-scope caches, safe waiting updates and fixture isolation.

### Working increments

1. **Audit and close specific gaps.** Map PRD release gates to existing evidence. Inspect fruit edges carried from M4/M5: queued fruit after collection as well as expiry; death clearing queued and active fruit without refiring consumed thresholds; freeze through GUESS/RESUMING and pause/background; reset on next level/restart; level-dependent score. Add meaningful simulation/integration cases for uncovered behavior using real game transitions. Retain collision ordering, no duplicate scoring, no-repeat words, extra life once, storage failures, interrupted input, ball reachability and all-dots-cleared solvability. Correct demonstrated defects with regressions; explain any proposed gameplay tuning using observed playtests and record decisions.
2. **Clean up and finalize documentation.** Remove only demonstrably obsolete code, debug output and misleading comments. Resolve the historical M1 lockfile metadata follow-up by checking current package/lock metadata (they presently appear aligned), not gratuitously regenerating dependencies. Correct AGENTS.md's stale statement that no executable checks exist. Preserve workflow rules and historical handoff findings. Rewrite README as a coherent current setup/game guide, with links to status/history instead of contradictory milestone snapshots. Cover concept, rules, keyboard/touch/pause/restart controls, pinned reproducible runtime, every actual setup/check/build/preview command, architecture, editing words/levels/maze/icons, original asset credits and existing Apache-2.0 license. Keep accurate subpath hosting, initial online preparation, install steps, safe updates, score-preserving scoped recovery versus full-origin reset, storage behavior and limitations. Do not claim current runtime/device verification from an older milestone's result.
3. **Rehearse from a fresh checkout.** Use an isolated checkout of the committed candidate with no inherited node_modules or build outputs. Follow the README verbatim on Node 22.12.0 (the deployment pin); record npm/OS versions and exact commands. Install the Playwright browser prerequisite when needed. Run the full checks below and verify documented dev and production preview entry points. Any unavailable runtime or failed command must remain explicit, with a resumption step; do not silently substitute a newer Node and call the minimum verified. Confirm clean production output, relative asset paths, fixture isolation and no unexpected dependency/lockfile churn. No existing lint script exists; do not invent a passing lint result.
The owner reports M5 device validation complete with no notes. Carry that evidence forward with accurate attribution; exact device/version details were not supplied. Recheck affected device behavior if M6 changes input/layout/PWA code. D024 removes the three recorded human playtests and demo rehearsal from scope and release acceptance; do not request run logs or block M6 on them. Pacing and performance targets remain targets unless measured.

### Acceptance criteria

| ID | Acceptance criterion | Required evidence |
| --- | --- | --- |
| AC1 | Required PRD features and all release gates are accounted for; inherited working behavior is preserved and no known blocking gameplay/PWA defect remains. | Gate-to-evidence table in this handoff, full regression results, explicit unresolved items. |
| AC2 | Remaining fruit/transition edges and any discovered release defect have meaningful coverage without production test hooks. | Named existing/new tests and what they exercise; relevant bug reproduction before/after when a fix is needed. |
| AC3 | A fresh checkout reproduces install, typecheck, unit/integration/browser checks and production build on documented Node 22.12.0. | Candidate SHA, clean-checkout procedure, exact commands/runtime/npm/OS and results; clean final diff. |
| AC4 | Production works at root and `/bhj-ai-hackathon/`, offline across five levels; updates preserve active/paused clients and failed downloads preserve the old build. | Retained root/subpath/offline/update browser suites against ordinary production; no fixtures or state setters in production. |
| AC5 | Final README and shared instructions describe the current project accurately and enable setup without chat history. | README checklist against PRD section 8, command rehearsal, credits/license and content-editing paths; stale status/verification claims corrected. |
| AC7 | Install/offline device evidence is carried forward accurately and relevant changes receive targeted revalidation; controls, larger text and reduced motion remain usable. | M5 owner report plus any new results, device details only where supplied; keyboard/touch/layout/focus/reduced-motion suites and explicit remaining limitations. |
| AC9 | Release candidate is reviewable and all evidence corresponds to identified revisions. | Full base/implementation SHAs, scope summary, check outcomes including skips/failures, changes outside checkpoint, remaining release gates and ownership. |

AC6 (three human playtests) and AC8 (demo rehearsal) were removed by D024; other criterion IDs remain stable.

### Required verification and exit check

Use the actual scripts, recording output counts rather than copying baseline numbers:

```sh
npm ci
npx playwright install chromium
npm run typecheck
npm test
npm run build
npm run build:fixture
npm run test:e2e
```

`test:e2e` already builds/previews its ordinary and fixture outputs; preserve that isolation. The accepted prior baseline is 300 unit/integration tests in 27 files and 98 browser tests with 3 intentional skips, independently run in M5 on Node 26.7.0; these are historical counts, not required fixed totals or fresh results. Confirm documented `npm run dev` and `npm run preview` work after their prerequisites. Keep retained evidence/screenshots from prior milestones intact unless an intentional reviewed change explains replacement; put new release evidence under `docs/evidence/m6/`.

Exit: a fresh checkout builds and runs, the remaining release-gate table has evidence and no blocking issue remains. Return REVIEW to Codex with a committed checkpoint; only Codex's review marks ACCEPTED. A deployment is not the exit criterion.

## Implementation handoff — Claude

### Revisions

- Implementation base: `8514be507ace8bc6df0067a4ec06bc87b92c2574` (the owner's planning commit "Accept M5 and define M6 release scope"; the checkout was clean, so no separate checkpoint was needed).
- Implementation commit (release candidate): `808be0f0d5d2a6cdd84a5de683fda311e797bfe9` on `main`.
- A later documentation-only commit records this section, `STATUS.md`, the rehearsal evidence in `docs/evidence/m6/`, and one README sentence added after the rehearsal: the browser suite rewrites committed screenshots, and `git checkout -- docs/evidence` restores them. It changes no application, test or configuration file.

### Summary

1. **Fruit edges.** Audited `src/game/game.ts`'s fruit handling against the brief's list. No defect was found, but five edges had no test. `tests/fruit.test.ts` gains a "fruit edges across transitions" block, driven only by real transitions (`steerPlayerTo`, ball capture, `guess`, `pause`/`resume`, a real lethal contact, `nextLevel`, `startLevel`):
   - a queued fruit appears on the substep after the first is collected, never the same one, with a full fresh lifetime, and scores once (appearing under the player scores nothing); no third fruit ever appears;
   - death clears a queued fruit as well as the active one, and neither threshold refires even after every remaining dot is eaten;
   - fruit lifetime is frozen through GUESS, RESUMING and a background (`'away'`) pause taken in each, and only CHASE spends it;
   - `nextLevel` clears a held level-one fruit, re-arms both thresholds and scores 100 × level (200 on level 2);
   - a restart from the pause overlay clears active and queued fruit and re-arms both thresholds.

   Each new test was confirmed not vacuous. Six temporary one-line mutations of `game.ts` each made exactly the targeted test fail, and `game.ts` was restored after each: death keeps the queue; the level reset keeps the queue; the queued fruit spawns on the collecting substep; fruit score ignores the level; the countdown ticks fruit; guessing ticks fruit.
2. **Cleanup.**
   - Removed `selectSeedWord` from `src/game/words.ts`. It was M2's word picker, superseded by M4's `selectWordForLevel`, and no application code referenced it. Its test in `tests/words.test.ts` was removed with it.
   - Corrected stale comments: `SEED_WORDS` "are M4 work", and `frameTiming.ts` "M3 replaces this".
   - `vite.config.ts`'s build-time `console.log` (the service-worker summary) is intentional build output and was kept.
   - M1-R3 lockfile follow-up: `package-lock.json`'s root name, version, license, engines and devDependencies match `package.json`, and every installed devDependency matches its exact pin. No regeneration was needed and none was done. **Closed.**
   - `AGENTS.md`: replaced the stale "no application or executable checks exist" statement with the actual scripts, and noted that no lint script exists.
3. **License (owner decision D025).** `LICENSE` held CC0 1.0 Universal from the initial commit, contradicting `package.json`, `package-lock.json` and this brief ("existing Apache-2.0 license"). Claude asked the owner, who chose Apache-2.0. `LICENSE` now holds the canonical text from apache.org (SHA-256 `cfc7749b…d30`). Package metadata was already `Apache-2.0` and is unchanged.
4. **README** is rewritten as a current setup and game guide. It links to STATUS, the PRD, DECISIONS and the handoffs instead of carrying milestone snapshots, and it no longer claims verification results from older milestones.
5. **Fresh-checkout rehearsal** on Node 22.12.0: see AC3.

### Release gate and criterion evidence

| PRD section 8 gate / criterion | Evidence | State |
| --- | --- | --- |
| Every required feature implemented or scope change documented | Section 2 table: maze/player (`tests/movement.test.ts`, `maze.test.ts`, `e2e/gameplay.spec.ts`); dots (`game.test.ts`); pellets (`pellets.test.ts`, `e2e/arcade.spec.ts`); four enemies/home (`enemy.test.ts`, `e2e/arcade.spec.ts`); fruit (`fruit.test.ts`, `e2e/campaign.spec.ts`); lives, score, best score, extra life, transitions (`lives.test.ts`, `score.test.ts`, `bestScore.test.ts`, `campaign.test.ts`); ball, words, guessing, five levels (`ball.test.ts`, `spawn.test.ts`, `words.test.ts`, `round.test.ts`, `e2e/round.spec.ts`, `e2e/campaign.spec.ts`). Supporting features: title/instructions, pause/restart (`pause.test.ts`, `e2e/arcade.spec.ts`), keyboard/touch (`input.test.ts`, `e2e/round.spec.ts`), responsive (`e2e/layout*.spec.ts`), offline/install metadata (`e2e/pwa*.spec.ts`), automated checks, README. Scope changes: audio (D016), human playtests/demo (D024). | Met |
| Fresh checkout installs and passes typecheck, unit/integration/browser tests and build | AC3 below | Met on Node 22.12.0, macOS arm64 |
| No known soft locks | Dots never end a level and the ball keeps running (`game.test.ts` "keeps gameplay active once the last dot is gone", `round.test.ts` "never completes a round by clearing the dots alone"); the word pool throws rather than loops (`words.test.ts`); a spawn with no eligible tile returns null rather than looping (`spawn.test.ts`); pause cannot overwrite its retained state (`pause.test.ts`); fruit queues cannot outlive death or level reset (new fruit tests). | Met; no known soft lock |
| No unreachable balls | Every corridor and dot reachable, home unreachable to the player and ball (`maze.test.ts`); spawn distance/fallback (`spawn.test.ts`); ball stays on legal tiles (`ball.test.ts`). | Met |
| No unintended duplicate scoring | Dot once (`game.test.ts`); duplicate guess ignored (`round.test.ts`); word bonus once (`round.test.ts`, `campaign.test.ts`); one eat per contact (`pellets.test.ts`); nothing awarded on a lethal slice (`collision.test.ts`); fruit once, including a queued fruit (`fruit.test.ts`); extra life once (`score.test.ts`). | Met |
| No wall penetration or stuck movement | `movement.test.ts` (long-step wall, buffered turns, reversal, tunnels); `loop.test.ts` (stalled frame); `collision.test.ts` (legal tiles only); held-input clearing (`input.test.ts`, `round.test.ts`). | Met |
| No forced mid-run updates | `e2e/pwa-update.spec.ts`: active, paused and title tabs undisturbed; a failed build goes `redundant`, leaving the old build offline-capable. Passed in the rehearsal. | Met |
| Real-device offline and installation check recorded; unavailable marked unverified | Carried forward: the owner reported M5 device validation complete with no notes (2026-09-24; see `handoffs/M5.md` acceptance). Exact devices and versions were not supplied. M6 changed no input, layout or PWA code, so no targeted device recheck was triggered. | Met as owner-reported; details unrecorded |
| Final README contents | AC5 below | Met |

| ID | Evidence | Assessment |
| --- | --- | --- |
| AC1 | Gate table above. The full regression passed in the fresh checkout: 304 unit/integration tests in 27 files, and 98 browser tests passed with 3 skips. The skips are the intentional touch-only journeys in the desktop project: `arcade.spec.ts:403`, `campaign.spec.ts:258`, `round.spec.ts:161`. No known blocking gameplay or PWA defect remains. | Met |
| AC2 | Five new `tests/fruit.test.ts` cases (Summary 1), each mutation-checked. They use real transitions and add no production hook: `src/` changed only by removing dead code and editing comments. Retained coverage for collision ordering, duplicate scoring, no-repeat words, the single extra life, storage failures, interrupted input, ball reachability and dots-cleared solvability is mapped in the gate table. No release defect in application code was found, so no fix was needed. | Met |
| AC3 | Clean clone of `808be0f` in an empty scratch directory, with no `node_modules` or build outputs (`docs/evidence/m6/00-env.txt`). Node 22.12.0 / npm 10.9.0, official tarball with checksum verified, macOS 15.7.9 arm64. `npm ci` (45 packages, 0 vulnerabilities), `npx playwright install chromium`, `npm run typecheck`, `npm test` (304/27 files), `npm run build`, `npm run build:fixture` and `npm run test:e2e` (98 passed, 3 skipped, 2.0 min) all exited 0. `npm run dev` → HTTP 200 on :5173. `npm run preview` → HTTP 200 for `/` and `/sw.js` on :4173. `dist/index.html` references only relative paths; there are no root-absolute references and no fixture parameter names in `dist/`, and `dist-fixture/` has no `sw.js`. Final clone `git status`: only the 64 evidence screenshots the browser suite rewrites; no package, lockfile or source churn. Logs are in `docs/evidence/m6/`. | Met |
| AC4 | Retained production suites passed in the rehearsal against the ordinary build: `pwa.spec.ts` (root precache, reload and fresh tab offline, missing asset not answered with HTML, never-online profile), `pwaSubpath.spec.ts` (`/bhj-ai-hackathon/` and a full five-level offline campaign), `pwa-update.spec.ts` (active, paused and title tabs protected; failed download keeps the old build), and `production.spec.ts` (no fixture controls). | Met |
| AC5 | The README covers concept, rules, controls (keyboard, touch, pause, restart), accessibility, requirements with the pinned range and the Node 22.12.0 reference, every script plus the Playwright prerequisite, hosting and subpath, first-online preparation, per-browser install steps, safe updates, storage and scoped recovery versus full-origin reset, architecture, editing words, levels, maze and icons, limitations, original asset credits and the license. Commands were rehearsed verbatim (AC3). `AGENTS.md`'s stale checks statement is corrected. | Met |
| AC7 | M5 owner report carried forward (gate table). No input, layout, focus, PWA or motion code changed in M6. Keyboard, touch, layout, focus and reduced-motion suites passed in the rehearsal: `input.test.ts`, `e2e/guessing.spec.ts`, `layout*.spec.ts`, `round.spec.ts` (touch), `reducedMotion.spec.ts`, `motion.test.ts`. | Met, with owner-reported device evidence |
| AC9 | SHAs above. Checks and skips are recorded in AC3. Changes outside the brief's expected areas: `LICENSE` and D025 (owner decision) and a comment in `src/app/frameTiming.ts`. | Met |

### Decisions and deviations

- D025 (owner): Apache License 2.0; `LICENSE` replaced. This is the only product-level decision.
- The brief called the license "existing Apache-2.0". The committed file was CC0, so the owner was asked rather than an assumption made either way.
- The browser suite's screenshot rewriting was documented in the README rather than changed, since changing how evidence is produced was outside cleanup scope and the brief asks that retained evidence stay intact.

### Limitations and unverified items

- Node 22.12.0 is verified by this rehearsal. The `^24` and `>=26` lines were not rerun in M6. Earlier, an ordinary working-tree typecheck and `npm test` passed on Node 26.7.0 before the candidate commit. Only macOS arm64 was exercised.
- The Playwright Chromium binary came from the existing user-level cache, not a fresh download.
- Device install and offline evidence is the owner's general M5 report, without device or browser versions. Pacing, 60 fps and OS text scaling are not measured. Human playtests and the demo rehearsal were removed by D024.
- No lint script exists; no lint result is claimed.
- Nothing was pushed or deployed. The Pages workflow would deploy on a push to `main`.

### Usage

- Claude elapsed session time: about 1.5 h (estimated from timestamps, not measured). Tokens were not measured.

### Next action

Codex reviews `8514be5..808be0f`, plus the documentation-only follow-up commit, against AC1–AC5, AC7 and AC9.

## Review round 1 — Codex

Pending. Record reviewed SHAs, independent versus reported verification, criterion assessment and findings with stable IDs, severity, file/line, impact and expected behavior. Append fix/review rounds if needed.

## Acceptance — Codex

Pending. Record accepted implementation SHA, evidence for every PRD release gate and any explicitly approved exceptions, remaining nonblocking limitations and STATUS update. Do not infer release approval or deployment authorization from a passing automated suite.
