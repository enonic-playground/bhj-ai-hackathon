# M6 — Release quality

## Brief — Codex

- State: IMPLEMENTING (Claude, 2026-09-24; implementation base `8514be507ace8bc6df0067a4ec06bc87b92c2574`).
- Owner: Claude (implementation); Codex reviews the committed checkpoint.
- Prepared: 2026-09-24.
- Specification: PRD v1.0 sections 5–10, AGENTS.md, D015/D016, D023 and D024.
- Repository/branch: `/Users/bhj/Documents/bhj-ai-hackathon`, `main`.
- Reference base commit: `0d9b83e6cc62dcd9ac04fd3cbc835145b61f16ef` (clean at planning start).
- Dependencies: M5 ACCEPTED following owner-reported device validation on 2026-09-24; M5-R1–R6 closed. Accepted application revision: `b9e25940d0aaf3361fdcb52e4c88620e08b0df48`, with reviewed README correction `1f0bd0f6c31bc800c764a2e10b811b656918b140`.
- Objective: a reproducible, documented, demo-ready release satisfying PRD section 8, with explicit evidence for every release gate.
- Estimate: retain the PRD allowance of **1.5–2 focused hours**, using existing contingency if needed. Active session time is included, not added; record Claude/Codex time and token usage separately if available. No new measured usage or revised overall estimate is claimed.

### Starting checkpoint and boundaries

Checkpoint this brief and the accompanying planning updates before application work, or use the owner's planning commit; record that full SHA as the actual implementation base. Mark IMPLEMENTING when starting. Preserve unfamiliar changes and keep every increment runnable.

In scope: targeted remaining regressions, fixes for demonstrated release defects, limited cleanup, final README and evidence, clean-install verification. Expected areas are `tests/`, `e2e/`, README, release evidence and narrowly affected source/configuration. Review existing coverage before adding tests; do not duplicate it to increase counts.

Out of scope: the three recorded human playtests and demo rehearsal (owner decision D024), new features, audio/music/mute, new mazes or modes, backend/accounts/analytics, saved runs, broad refactors, unrelated dependency upgrades, changing accepted rules, public publishing, tagging a release or changing hosting settings. The existing Pages workflow deploys on pushes to `main`; do not push or deploy under this brief. Preserve the approved normal-motion feedback and reduced-motion behavior, exact-scope caches, safe waiting updates and fixture isolation.

### Working increments

1. **Audit and close specific gaps.** Map PRD release gates to existing evidence. Inspect fruit edges carried from M4/M5: queued fruit after collection as well as expiry; death clearing queued and active fruit without refiring consumed thresholds; freeze through GUESS/RESUMING and pause/background; reset on next level/restart; level-dependent score. Add meaningful simulation/integration cases for uncovered behavior using real game transitions. Retain collision ordering, no duplicate scoring, no-repeat words, extra life once, storage failures, interrupted input, ball reachability and all-dots-cleared solvability. Correct demonstrated defects with regressions; explain any proposed gameplay tuning using observed playtests and record decisions.
2. **Clean up and finalize documentation.** Remove only demonstrably obsolete code, debug output and misleading comments. Resolve the historical M1 lockfile metadata follow-up by checking current package/lock metadata (they presently appear aligned), not gratuitously regenerating dependencies. Correct AGENTS.md's stale statement that no executable checks exist. Preserve workflow rules and historical handoff findings. Rewrite README as a coherent current setup/game guide, with links to status/history instead of contradictory milestone snapshots. Cover concept, rules, keyboard/touch/pause/restart controls, pinned reproducible runtime, every actual setup/check/build/preview command, architecture, editing words/levels/maze/icons, original asset credits and existing Apache-2.0 license. Keep accurate subpath hosting, initial online preparation, install steps, safe updates, score-preserving scoped recovery versus full-origin reset, storage behavior and limitations. Do not claim current runtime/device verification from an older milestone's result.
3. **Rehearse from a fresh checkout.** Use an isolated checkout of the committed candidate with no inherited node_modules or build outputs. Follow the README verbatim on Node 22.12.0 (the deployment pin); record npm/OS versions and exact commands. Install the Playwright browser prerequisite when needed. Run the full checks below and verify documented dev and production preview entry points. Any unavailable runtime or failed command must remain explicit, with a resumption step; do not silently substitute a newer Node and call the minimum verified. Confirm clean production output, relative asset paths, fixture isolation and no unexpected dependency/lockfile churn. No existing lint script exists; do not invent a passing lint result.
The owner reports M5 device validation complete with no notes. Carry that evidence forward with accurate attribution; exact device/version details were not supplied. Recheck affected device behavior if M6 changes input/layout/PWA code. D024 removes the three recorded human playtests and demo rehearsal from scope and release acceptance; do not request run logs or block M6 on them. Pacing and performance targets remain targets unless measured.

### Acceptance criteria

| ID | Acceptance criterion | Required evidence |
| --- | --- | --- |
| AC1 | Required PRD features and all release gates are accounted for; inherited working behavior is preserved and no known blocking gameplay/PWA defect remains. | Gate-to-evidence table in this handoff, full regression results, explicit unresolved items. |
| AC2 | Remaining fruit/transition edges and any discovered release defect have meaningful coverage without production test hooks. | Named existing/new tests and what they exercise; relevant bug reproduction before/after when a fix is needed. |
| AC3 | A fresh checkout reproduces install, typecheck, unit/integration/browser checks and production build on documented Node 22.12.0. | Candidate SHA, clean-checkout procedure, exact commands/runtime/npm/OS and results; clean final diff. |
| AC4 | Production works at root and `/bhj-ai-hackathon/`, offline across five levels; updates preserve active/paused clients and failed downloads preserve the old build. | Retained root/subpath/offline/update browser suites against ordinary production; no fixtures or state setters in production. |
| AC5 | Final README and shared instructions describe the current project accurately and enable setup without chat history. | README checklist against PRD section 8, command rehearsal, credits/license and content-editing paths; stale status/verification claims corrected. |
| AC7 | Install/offline device evidence is carried forward accurately and relevant changes receive targeted revalidation; controls, larger text and reduced motion remain usable. | M5 owner report plus any new results, device details only where supplied; keyboard/touch/layout/focus/reduced-motion suites and explicit remaining limitations. |
| AC9 | Release candidate is reviewable and all evidence corresponds to identified revisions. | Full base/implementation SHAs, scope summary, check outcomes including skips/failures, changes outside checkpoint, remaining release gates and ownership. |

AC6 (three human playtests) and AC8 (demo rehearsal) were removed by D024; other criterion IDs remain stable.

### Required verification and exit check

Use the actual scripts, recording output counts rather than copying baseline numbers:

```sh
npm ci
npx playwright install chromium
npm run typecheck
npm test
npm run build
npm run build:fixture
npm run test:e2e
```

`test:e2e` already builds/previews its ordinary and fixture outputs; preserve that isolation. The accepted prior baseline is 300 unit/integration tests in 27 files and 98 browser tests with 3 intentional skips, independently run in M5 on Node 26.7.0; these are historical counts, not required fixed totals or fresh results. Confirm documented `npm run dev` and `npm run preview` work after their prerequisites. Keep retained evidence/screenshots from prior milestones intact unless an intentional reviewed change explains replacement; put new release evidence under `docs/evidence/m6/`.

Exit: a fresh checkout builds and runs, the remaining release-gate table has evidence and no blocking issue remains. Return REVIEW to Codex with a committed checkpoint; only Codex's review marks ACCEPTED. A deployment is not the exit criterion.

## Implementation handoff — Claude

Pending. Record actual base and implementation SHAs, summary, evidence for active criteria AC1–AC5, AC7 and AC9, exact commands/results and startup/preview checks, decisions/deviations, unresolved limitations, changes outside the checkpoint, next action and separate measured/estimated/unavailable work/session/token figures.

## Review round 1 — Codex

Pending. Record reviewed SHAs, independent versus reported verification, criterion assessment and findings with stable IDs, severity, file/line, impact and expected behavior. Append fix/review rounds if needed.

## Acceptance — Codex

Pending. Record accepted implementation SHA, evidence for every PRD release gate and any explicitly approved exceptions, remaining nonblocking limitations and STATUS update. Do not infer release approval or deployment authorization from a passing automated suite.
